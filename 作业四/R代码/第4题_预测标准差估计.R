# 第4题：用统计学习方法预测某特定X值对应Y时如何估计预测标准差
# 高级统计方法 第4次作业
# 张鸿昊 20242081353

# 本题为概念分析题，无需R代码
# 方法：使用自助法(Bootstrap)。
# 步骤：(1)对原始数据集进行B次有放回抽样，得到B个自助样本；
#       (2)在每个自助样本上拟合模型，对该特定X值作出预测，得到B个预测值；
#       (3)这B个预测值的标准差即为该预测的标准差估计。
# 自助法不依赖模型的分布假设，适用于复杂模型(如KNN、树等)的预测标准差估计。

cat("第4题为概念题，详见Word文档\n")
