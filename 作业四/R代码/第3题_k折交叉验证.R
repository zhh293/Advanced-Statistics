# 第3题：复习k折交叉验证
# 高级统计方法 第4次作业
# 张鸿昊 20242081353

# 本题为概念分析题，无需R代码
# (a) k折交叉验证步骤：将数据随机分成k个大小相近的折(fold)；
#     每次留一折作验证集、其余k-1折作训练集，计算验证误差；重复k次后取平均。
# (b) k折交叉验证 vs 验证集方法：偏差更小、估计更稳定、利用了全部数据；计算量更大。
#     k折交叉验证 vs LOOCV：计算量小得多(k次 vs n次)，方差更低(折间相关性低)，
#                          但偏差略高于LOOCV。

cat("第3题为概念题，详见Word文档\n")
