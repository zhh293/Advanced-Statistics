# 第4题：岭回归随lambda增大各量的变化（概念题）
# 高级统计方法 第5次作业
# 张鸿昊 20242081353
#
# 岭回归: minimize  RSS + lambda * sum(beta_j^2)
# 当lambda从0开始增大（惩罚加重，系数被压缩，模型越来越不灵活）：
# 注意：与第3题的s方向相反！s增大=放松约束，lambda增大=加重约束。
#
# (a) 训练RSS：iii. 稳定增大。lambda增大→系数受限→训练拟合变差。
# (b) 测试RSS：ii. 先减后增（U型）。先因方差下降而减小，
#              后因偏差过大（欠拟合）而回升。
# (c) 方差：    iv. 稳定减小。lambda增大→系数收缩→方差减小。
# (d) 偏差^2：  iii. 稳定增大。lambda增大→系数偏离最小二乘→偏差增大。
# (e) 不可约误差：v. 保持不变。与模型无关。
