# 第3题：lasso约束形式下各量随s增大的变化（概念题）
# 高级统计方法 第5次作业
# 张鸿昊 20242081353
#
# lasso: minimize RSS  subject to  sum|beta_j| <= s
# 当s从0开始增大（约束逐渐放松，模型越来越灵活）：
#
# (a) 训练RSS：iv. 稳定减小。s增大→模型更灵活→训练拟合越来越好。
# (b) 测试RSS：ii. 先减后增（U型）。先因偏差下降而减小，
#              后因方差增大（过拟合）而回升。
# (c) 方差：    iii. 稳定增大。模型越灵活方差越大。
# (d) 偏差^2：  iv. 稳定减小。模型越灵活偏差越小。
# (e) 不可约误差：v. 保持不变。不可约误差是数据固有噪声，与模型无关。
