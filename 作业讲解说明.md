# 高级统计方法 第2次作业 讲解说明

**学号：** 20242081353  
**姓名：** 张鸿昊  
**班级：** 软件2410  
**作业章节：** 《统计学习导论》第2.4节 习题1-4（概念题）、7-10（应用题）

---

## 文件夹结构

```
高统作业/
├── 1-张鸿昊-第2次作业.docx     ← 最终提交的作业文档（含图片）
├── 作业讲解说明.md              ← 本文件：代码对应关系 + 讲解
├── R代码/
│   ├── 第7题_KNN距离计算.R
│   ├── 第8题_College数据集探索.R
│   ├── 第9题_Auto数据集分析.R
│   └── 第10题_Boston数据集分析.R
└── 图片输出/
    ├── q7_knn示意.png
    ├── q8_散点图矩阵.png
    ├── q8_学费箱线图.png
    ├── q8_精英大学学费.png
    ├── q8_直方图.png
    ├── q9_散点图矩阵.png
    ├── q9_mpg与weight关系.png
    ├── q9_相关系数.png
    ├── q10_散点图矩阵.png
    ├── q10_犯罪率关联分析.png
    ├── q10_犯罪率分布.png
    └── q10_房间数与房价.png
```

---

## 概念题讲解（第1-4题）

### 第1题：灵活方法 vs 不灵活方法

**核心知识点：偏差-方差权衡（Bias-Variance Tradeoff）**

这道题考察的是在不同数据条件下，应该选择灵活（flexible）还是不灵活（inflexible）的统计学习方法。

- **灵活方法**（如多项式回归、样条、随机森林）：偏差低、方差高，需要大量数据才能稳定估计
- **不灵活方法**（如线性回归、LDA）：偏差高、方差低，在小样本或高噪声时更稳健

| 情形 | 推荐方法 | 原因 |
|------|---------|------|
| n大、p小 | 灵活 | 数据充足，可以安全地拟合复杂模型 |
| n小、p大 | 不灵活 | 参数多于样本，灵活方法会严重过拟合 |
| 高度非线性 | 灵活 | 线性模型无法捕捉非线性关系，偏差太大 |
| 误差方差大 | 不灵活 | 噪声大时灵活方法会把噪声也拟合进去 |

### 第2题：回归 vs 分类

**核心知识点：响应变量的类型决定问题类型**

- 响应变量是**连续数值** → 回归（Regression）
- 响应变量是**类别标签** → 分类（Classification）

另外要区分**预测（prediction）**和**推断（inference）**：
- 预测：关心模型输出的准确性，不在乎可解释性
- 推断：关心哪些变量影响响应变量，需要可解释性

### 第3题：偏差-方差权衡图

**核心知识点：理解5条曲线的形状**

随着模型灵活性（自由度）增加：
1. **偏差²（Bias²）**：单调递减——模型越复杂，对真实关系的假设越少，偏差越小
2. **方差（Variance）**：单调递增——模型越复杂，对训练数据越敏感，方差越大
3. **训练误差**：单调递减——灵活模型总能把训练数据拟合得更好
4. **测试误差**：U形——偏差和方差共同决定，存在最优点
5. **不可约误差（Irreducible Error）**：水平线——是数据本身的噪声，任何模型都无法消除

**测试误差 = 偏差² + 方差 + 不可约误差**

### 第4题：三类问题的实际应用

这道题要求举出分类、回归、聚类的实际例子，并说明是预测还是推断。

**关键区别：**
- 分类和回归都是**监督学习**（有标签）
- 聚类是**无监督学习**（无标签，自动发现结构）

---

## 应用题讲解（第7-10题）

### 第7题：KNN分类

**对应代码文件：** `R代码/第7题_KNN距离计算.R`

**核心知识点：K近邻算法（K-Nearest Neighbors）**

KNN的工作原理：
1. 计算测试点到所有训练点的距离（通常用欧氏距离）
2. 找到最近的K个邻居
3. 多数投票决定类别（分类）或取均值（回归）

**欧氏距离公式：**
$$d = \sqrt{(x_1-x_1')^2 + (x_2-x_2')^2 + (x_3-x_3')^2}$$

**代码逻辑解释：**
```r
# 构建数据框，包含6个训练观测
obs <- data.frame(
  X1=c(0,2,0,0,-1,1), X2=c(3,0,1,1,0,1), X3=c(0,0,3,2,1,1),
  Y=c("Red","Red","Red","Green","Green","Red"))

# 计算到原点(0,0,0)的欧氏距离
obs$dist <- sqrt(obs$X1^2 + obs$X2^2 + obs$X3^2)
# 结果：3.000, 2.000, 3.162, 2.236, 1.414, 1.732

# K=1：取最近的1个邻居
which.min(obs$dist)  # 第5个观测，距离1.414，类别Green

# K=3：取最近的3个邻居，多数投票
sorted_idx <- order(obs$dist)  # 排序：5,6,2,4,1,3
k3_idx <- sorted_idx[1:3]      # 取前3：5(Green), 6(Red), 2(Red)
# 2票Red vs 1票Green → 预测Red
```

**为什么非线性边界时K要小？**
K小 → 决策边界复杂（低偏差高方差）→ 能拟合非线性边界
K大 → 决策边界平滑（高偏差低方差）→ 趋向线性，无法拟合复杂边界

---

### 第8题：College数据集探索性分析

**对应代码文件：** `R代码/第8题_College数据集探索.R`

**核心知识点：探索性数据分析（EDA）**

**数据集：** ISLR2包中的College数据集，777所美国大学，18个变量

**代码逻辑解释：**

```r
library(ISLR2)
data(College)

# (b) 查看数据
head(College)      # 查看前6行，了解数据结构
summary(College)   # 数值摘要：最小值、四分位数、均值、最大值

# (c.ii) 散点图矩阵：观察变量间的两两关系
pairs(College[, 1:10])  # 只看前10列，避免图太密

# (c.iii) 箱线图：比较私立vs公立大学的学费
plot(College$Private, College$Outstate,
     xlab="Private", ylab="Out-of-State Tuition")
# 结论：私立大学学费显著更高

# (c.iv) 创建Elite变量：Top10perc>50%的为精英大学
Elite <- rep("No", nrow(College))
Elite[College$Top10perc > 50] <- "Yes"
Elite <- as.factor(Elite)
College <- data.frame(College, Elite)
summary(Elite)  # No:699, Yes:78

# (c.v) 直方图：观察变量分布形态
par(mfrow=c(2,2))  # 2×2布局
hist(College$Apps, breaks=50)    # 申请人数：右偏
hist(College$Accept, breaks=50)  # 录取人数：右偏
hist(College$Enroll, breaks=50)  # 注册人数：右偏
hist(College$Outstate, breaks=50) # 学费：相对均匀
```

**为什么要做EDA？**
在建模之前，EDA帮助我们：
1. 了解数据的分布形态（正态/偏态/双峰）
2. 发现异常值和缺失值
3. 观察变量间的相关关系
4. 为后续建模选择合适的方法

---

### 第9题：Auto数据集分析

**对应代码文件：** `R代码/第9题_Auto数据集分析.R`

**核心知识点：变量类型识别、描述统计、相关分析**

**数据集：** ISLR2包中的Auto数据集，392辆汽车，9个变量

**代码逻辑解释：**

```r
library(ISLR2)
data(Auto)

# (a) 识别变量类型
# 定量：mpg, cylinders, displacement, horsepower, weight, acceleration, year
# 定性：origin（1=美/2=欧/3=日，类别变量）, name（车型名称）

# (b) 各变量范围
sapply(Auto[,1:7], range)
# sapply对每列应用range函数，返回[最小值, 最大值]

# (c) 均值和标准差
sapply(Auto[,1:7], mean)
sapply(Auto[,1:7], sd)

# (d) 移除第10-85行后的统计
Auto_sub <- Auto[-(10:85), ]  # 负索引：去掉第10到85行，共76行
# 剩余316行，统计量变化不大

# (e) 探索性分析
pairs(Auto[, 1:7])  # 散点图矩阵

# (f) 与mpg相关的变量
cor(Auto[, 1:7])["mpg", ]  # 计算mpg与其他变量的相关系数
# weight(r=-0.832)最强负相关 → 车越重，油耗越高（mpg越低）
# year(r=0.581)正相关 → 新款车更省油
```

**相关系数的解读：**
- r接近1：强正相关
- r接近-1：强负相关
- r接近0：弱相关或无线性相关
- |r|>0.7通常认为是强相关

---

### 第10题：Boston数据集分析

**对应代码文件：** `R代码/第10题_Boston数据集分析.R`

**核心知识点：多变量数据探索、条件筛选、异常值分析**

**数据集：** ISLR2包中的Boston数据集，506个波士顿郊区，13个变量

**代码逻辑解释：**

```r
library(ISLR2)
data(Boston)
dim(Boston)  # 506行，13列

# (b) 散点图矩阵
pairs(Boston)  # 观察所有变量间的两两关系

# (c) 与犯罪率相关的变量
cor(Boston)["crim", ]  # 计算crim与所有变量的相关系数
# rad(r=0.626)：公路可达性高 → 犯罪率高（交通便利利于逃跑？）
# tax(r=0.583)：税率高 → 犯罪率高（贫困地区？）
# medv(r=-0.388)：房价高 → 犯罪率低（富裕地区治安好）

# (d) 范围分析
range(Boston$crim)    # [0.006, 88.976]，差异极大
range(Boston$tax)     # [187, 711]
range(Boston$ptratio) # [12.6, 22.0]

# (e) 临河郊区数量
sum(Boston$chas == 1)  # 35个郊区临近查尔斯河

# (f) 师生比中位数
median(Boston$ptratio)  # 19.05

# (g) 房价最低的郊区
Boston[which.min(Boston$medv), ]
# which.min返回最小值的行索引
# 结果：高犯罪率、高税率、高师生比、高低收入比例

# (h) 大房间数郊区
sum(Boston$rm > 7)  # 64个郊区平均房间数>7
sum(Boston$rm > 8)  # 13个郊区平均房间数>8
summary(Boston[Boston$rm > 8, ])  # 这些郊区的特征：低犯罪、高房价
```

**数据分析的思路：**
1. 先了解数据结构（dim, summary, head）
2. 可视化探索（pairs, hist, plot）
3. 量化关系（cor相关系数）
4. 条件筛选找极端情况（which.min, 逻辑索引）
5. 综合解读，给出有意义的结论

---

## 运行环境

- **R版本：** R 4.6.0
- **R包：** ISLR2（需要先安装：`install.packages("ISLR2")`）
- **运行方式：** 在R或RStudio中直接运行各R脚本文件

```r
# 安装依赖包（只需运行一次）
install.packages("ISLR2")

# 然后运行各题代码文件即可
```

---

