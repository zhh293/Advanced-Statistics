# 研究生学习工作汇报

**汇报人：** 张宏浩  
**汇报日期：** 2025年7月  
**指导教师：** [导师姓名]  

---

## 一、本阶段学习任务概述

本阶段主要围绕三维生成、多孔结构优化以及物理驱动视频生成三个方向展开文献阅读与项目复现工作，具体内容包括：

1. 论文阅读：**TopoGen: Topology-Aware 3D Generation with Persistence Points**
2. 论文阅读：**RBF-PoroNet: A Differentiable Implicit Framework for Automated Optimization of Porous Structures via a Self-Adaptive RBF Neural Network**
3. 开源项目复现：**PhysGen: Rigid-Body Physics-Grounded Image-to-Video Generation**（ECCV 2024）

---

## 二、论文阅读笔记

### 2.1 TopoGen: Topology-Aware 3D Generation with Persistence Points

**发表信息：** Computer Graphics Forum (CGF), 2025  
**作者：** Jiangbei Hu, Ben Fei, Baixin Xu, Fei Hou, Weidong Yang, Shengfa Wang, Na Lei, Chen Qian, Ying He  
**单位：** 南洋理工大学、香港中文大学、中科院软件所、大连理工大学等  
**论文链接：** https://onlinelibrary.wiley.com/doi/10.1111/cgf.70257  
**arXiv预印本：** https://arxiv.org/abs/2401.17603

#### 2.1.1 研究背景与动机

当前基于扩散模型的三维形状生成方法（如3D-LDM、DiffComplete等）在几何质量上已取得显著进展，但普遍忽略了生成结果的**拓扑特征**。例如，生成一个杯子时，模型可能生成出没有把手孔洞的实心形状，或者生成带有多余拓扑连通分量的异常结构。这类拓扑错误在工程设计、3D打印等下游应用中是不可接受的。

传统方法大多只关注局部几何特征（如点云密度、法向量一致性），缺乏对全局拓扑结构的显式建模和控制能力。TopoGen正是为了解决这一问题而提出——如何在三维生成过程中引入拓扑感知能力。

#### 2.1.2 核心方法

TopoGen的核心思想是利用**持久同调（Persistent Homology）**从三维形状中提取拓扑描述子，并将其作为条件信息注入生成模型。具体包括：

**（1）拓扑特征提取**

- **Betti数（Betti Numbers）：** 离散的拓扑不变量，$\beta_0$ 表示连通分量数量，$\beta_1$ 表示一维环/隧道数量，$\beta_2$ 表示二维空腔/腔洞数量。例如一个环面（torus）有 $\beta_0=1, \beta_1=2, \beta_2=1$。
- **持久点（Persistence Points）：** 连续的拓扑描述子，来源于持久图（Persistence Diagram）。每个持久点 $(b_i, d_i)$ 记录一个拓扑特征的"出生"和"死亡"时刻，其存活时间 $d_i - b_i$ 反映该特征的显著程度。

论文将三维形状表示为隐式场（implicit field），通过对隐式场施加不同的阈值进行子水平集过滤（sublevel set filtration），从而计算持久同调。

**（2）拓扑感知的潜空间扩散**

- **自编码器（Autoencoder）：** 采用基于Transformer的自编码模块，将每个三维形状的隐式表示编码为一组潜向量（latent vectors）。
- **条件扩散模型（Conditioned Diffusion Model）：** 在潜空间中进行扩散生成，关键创新在于将 Betti 数和持久点作为条件信息策略性地注入扩散过程，使模型能够生成具有指定拓扑结构的三维形状。

**（3）灵活的条件输入**

该框架支持多种条件输入模式：稀疏点云、部分点云、草图等。通过修改持久图，用户可以改变从这些输入模态生成的形状的拓扑结构，例如从同一个点云输入生成不同数量孔洞的形状。

#### 2.1.3 主要贡献

- 首次将持久同调中的离散（Betti数）和连续（持久点）拓扑描述子同时引入三维生成模型；
- 提出了拓扑条件化的潜空间扩散生成框架，实现了对生成形状拓扑结构的显式控制；
- 框架具有良好的通用性，支持多种条件输入模态，并允许用户通过修改拓扑描述子来编辑生成结果。

#### 2.1.4 个人思考

TopoGen的核心启示在于：**拓扑信息可以作为一种高层次的结构性先验来指导生成模型。** 这与传统只关注几何细节的生成方法形成互补。在实际应用中，拓扑正确性往往比局部几何精度更为关键（如3D打印中的可制造性分析）。未来可以考虑将类似的拓扑感知思路扩展到其他生成任务，如分子生成、拓扑优化等领域。

---

### 2.2 RBF-PoroNet: A Differentiable Implicit Framework for Automated Optimization of Porous Structures

**发表信息：** Additive Manufacturing, 2025（ScienceDirect）  
**作者：** H. Du, S. Wang 等（大连理工大学智能网格与工业软件实验室）  
**论文链接：** https://www.sciencedirect.com/science/article/pii/S2214860426001181  
**院校动态：** https://ss.dlut.edu.cn/info/1541/32282.htm

#### 2.2.1 研究背景与动机

三重周期极小曲面（TPMS, Triply Periodic Minimal Surfaces）多孔结构因其优异的力学性能和可控的孔隙率，在轻量化设计、骨组织工程、散热器等领域有广泛应用。然而，传统TPMS多孔结构的设计和优化面临以下挑战：

- **建模与优化分离：** 传统方法通常先手动选定TPMS类型和参数，再进行有限元分析和优化迭代，流程繁琐且难以实现端到端自动化。
- **梯度不可用：** 传统隐式建模方法（如水平集方法）在优化过程中难以高效计算梯度，限制了梯度优化方法的应用。
- **多物理场耦合困难：** 实际工程中需要同时考虑力学、热学、流体等多物理场，传统方法在多目标协同优化方面能力有限。

#### 2.2.2 核心方法

RBF-PoroNet 提出了一个全新的**全可微隐式优化框架**，核心创新是引入自适应径向基函数神经网络（SA-RBFNN）来实现TPMS多孔结构的隐式多尺度建模与自动化优化。

**（1）自适应RBF神经网络（SA-RBFNN）**

与传统的固定基函数RBF网络不同，SA-RBFNN 的关键特性包括：

- **自适应中心与宽度：** 网络中RBF节点的中心位置和带宽参数均为可学习参数，可以根据优化目标自适应地调整空间分辨率。
- **隐式曲面表示：** 将TPMS多孔结构表示为RBF网络的零水平集，网络输入为空间坐标 $(x, y, z)$，输出为隐式场值 $f(x,y,z)$，零水平面 $f=0$ 即为结构表面。
- **全可微性：** 整个建模过程基于神经网络，天然支持自动微分（AutoDiff），可以通过反向传播直接计算结构参数对物理性能的梯度。

**（2）端到端优化流程**

框架的优化流程为：

- **前向传播：** SA-RBFNN生成隐式场 → 提取结构表面 → 有限元分析计算物理性能（刚度、热导率等）；
- **反向传播：** 通过自动微分计算物理性能对网络参数的梯度 → 梯度下降更新SA-RBFNN参数；
- **迭代优化：** 重复上述过程直至满足设计约束和优化目标。

**（3）多物理场多尺度优化**

该框架支持：
- 拓扑优化（决定哪里有材料/哪里是孔隙）
- 几何优化（精细调整孔隙的形状和连通性）
- 多物理场目标（同时优化力学性能与热传导性能等）

#### 2.2.3 主要贡献

- 提出SA-RBFNN作为TPMS多孔结构的全可微隐式表示，实现了建模与优化的统一框架；
- 首次实现面向TPMS多孔结构的全自动、端到端的拓扑与几何协同优化；
- 框架支持多物理场耦合优化，在力学-热学等多目标场景中展现了优异性能；
- 与传统优化方法相比，计算效率和优化质量均有显著提升。

#### 2.2.4 个人思考

RBF-PoroNet的核心贡献在于**将神经网络的可微性优势引入传统的结构优化问题**。这一思路与近年来"可微渲染""可微物理仿真"等方向的发展一脉相承——通过让计算图全程可微，将传统的"分析-设计-验证"迭代流程转化为端到端的梯度优化问题。

与TopoGen的联系：两篇论文都涉及三维结构的隐式表示，但关注点不同——TopoGen关注生成多样性和拓扑控制，RBF-PoroNet关注性能优化和可制造性。未来是否可以将TopoGen的拓扑感知能力与RBF-PoroNet的性能优化能力结合，实现"拓扑感知的多孔结构自动化设计"，是一个值得探索的方向。

---

## 三、开源项目复现：PhysGen

### 3.1 项目基本信息

**论文：** PhysGen: Rigid-Body Physics-Grounded Image-to-Video Generation  
**发表：** ECCV 2024  
**作者：** Shaowei Liu, Zhongzheng Ren, Saurabh Gupta, Shenlong Wang  
**单位：** 伊利诺伊大学厄巴纳-香槟分校（UIUC）  
**GitHub：** https://github.com/stevenlsw/physgen  
**项目主页：** https://stevenlsw.github.io/physgen/  
**arXiv：** https://arxiv.org/abs/2409.18964

### 3.2 研究动机与核心思想

现有的图像到视频（Image-to-Video）生成方法大多是纯数据驱动的，虽然能生成视觉上看起来合理的视频，但在物理真实性方面存在明显不足：物体可能穿透墙壁、违反重力、或做出不符合物理规律的运动。

PhysGen提出的核心思想是：**将基于模型的物理仿真与数据驱动的视频扩散模型相结合**，实现一种training-free（无需额外训练）的图像到视频生成管线。用户只需提供一张静态图像和物理条件（如作用于物体的力和扭矩），即可生成既视觉逼真又物理正确的视频。

### 3.3 系统架构

PhysGen的管线分为三个核心阶段：

**阶段一：感知（Perception）**

感知模块负责从输入图像中提取所有后续阶段所需的信息：

- **物体分割（Grounded-Segment-Anything）：** 识别并分割图像中的物体，生成 `mask.png`
- **深度与法向量估计（GeoWizard）：** 估计场景的深度图 `depth.npy` 和法向量图 `normal.npy`
- **背景修复（Inpaint-Anything + LaMa）：** 对前景物体遮挡的背景区域进行修复，生成 `inpaint.png`
- **反照率与着色分解（Intrinsic）：** 将图像分解为反照率和着色分量 `shading.npy`
- **物理属性推理（GPT-4V）：** 利用GPT-4V视觉模型推理物体的物理属性（质量、摩擦系数、弹性等），生成 `physics.yaml`

**阶段二：仿真（Simulation）**

仿真模块在图像空间中进行刚体物理模拟：

- 使用 **Pymunk**（Chipmunk2D 的 Python 封装）作为2D物理引擎
- 根据感知阶段推理的物理参数和用户输入的力/扭矩条件，模拟物体在图像平面内的运动
- 处理碰撞检测与响应、重力作用、摩擦力等
- 输出仿真历史 `history.pkl` 和合成视频 `composite.mp4`

**阶段三：渲染（Rendering）**

渲染模块进一步提升视觉质量：

- **重打光（Relighting）：** 根据物体运动后法向量的变化重新计算光照，使光影随运动变化保持一致
- **视频扩散精修（SEINE Video Diffusion）：** 利用预训练的视频扩散模型（SEINE，基于Stable Diffusion v1.4）对仿真合成视频进行精修，补充细节纹理、消除合成伪影，生成最终的高质量视频 `final_video.mp4`

### 3.4 复现过程

#### 环境配置

```bash
# 1. 克隆仓库（含子模块）
git clone --recurse-submodules https://github.com/stevenlsw/physgen.git
cd physgen

# 2. 创建 conda 环境
conda create -n physgen python=3.9
conda activate physgen

# 3. 安装依赖
pip install -r requirements.txt

# 4. 设置 PYTHONPATH
export PYTHONPATH=$(pwd)
```

#### Quick Demo 运行

PhysGen 提供了快速演示模式，**无需 GPU 或显示设备**，仅需3秒即可运行图像空间动力学仿真：

```bash
name="pool"
python simulation/animate.py --data_root data --save_root outputs --config data/${name}/sim.yaml
```

项目内置了5个示例场景：`pool`（台球）、`domino`（多米诺骨牌）、`balls`（弹跳球）、`pig_ball`（小猪与球）、`car`（玩具车）。

#### 完整管线运行

```bash
# 一键运行仿真+重打光+视频扩散
bash scripts/run_demo.sh ${name}
```

完整管线需要预先下载 SEINE 视频扩散模型：

```bash
mkdir -p diffusion/SEINE/pretrained
git clone https://huggingface.co/CompVis/stable-diffusion-v1-4 diffusion/SEINE/pretrained/stable-diffusion-v1-4
wget -P diffusion/SEINE/pretrained https://huggingface.co/Vchitect/SEINE/resolve/main/seine.pt
```

#### 自定义图像处理

对自定义图像的完整处理流程为：

```bash
# 感知阶段
cd perception/
python gpt_ram.py --img_path ${custom_folder}
python run_gsam.py --input ${custom_folder}
python run_depth_normal.py --input ${custom_folder} --vis
python run_fg_bg.py --input ${custom_folder} --vis_edge
python run_inpaint.py --input ${custom_folder} --dilate_kernel_size 20
python run_albedo_shading.py --input ${custom_folder} --vis

# 手动创建 sim.yaml 配置仿真参数

# 仿真阶段
cd simulation/
python animate.py --data_root ${custom_folder} --save_root ${custom_folder} --config ${custom_folder}/sim.yaml

# 渲染阶段
cd relight/
python relight.py --perception_input ${custom_folder} --previous_output ${custom_folder}
cd ../diffusion/
python video_diffusion.py --perception_input ${custom_folder} --previous_output ${custom_folder}
```

### 3.5 外部依赖一览

| 组件 | 用途 | 集成方式 |
|------|------|----------|
| Grounded-Segment-Anything | 物体检测与分割 | Git submodule |
| GeoWizard | 深度/法向量估计 | Git submodule |
| Inpaint-Anything + LaMa | 背景修复 | Git submodule |
| Intrinsic | 反照率/着色分解 | 外部库 |
| GPT-4V | 物理属性推理 | 外部API |
| Pymunk | 2D刚体物理仿真 | pip 安装 |
| SEINE | 视频扩散精修 | Git submodule |

### 3.6 复现结果与分析

Quick Demo 阶段的仿真可以顺利运行，物体运动轨迹符合刚体物理预期。台球碰撞场景中可以观察到动量守恒和能量衰减的合理表现。完整管线中 SEINE 视频扩散精修能有效消除合成伪影，使最终视频在视觉质量上接近真实拍摄效果。

**项目优势：**
- Training-free 设计，无需昂贵的视频数据训练
- 模块化架构，各阶段可独立运行和调试
- 物理可控性强，用户可精确指定力/扭矩条件

**当前局限：**
- 仅支持刚体物理，不支持柔体/流体
- 感知模块依赖 GPT-4V API，存在成本和访问限制
- 仅适用于侧视图和俯视图场景，对复杂视角效果有限

### 3.7 个人思考

PhysGen 的设计哲学非常值得学习：**不重新训练模型，而是通过精巧的系统设计将多个预训练模型和物理引擎组合成一个连贯的管线。** 这种 "training-free pipeline" 的思路在大模型时代具有重要价值——充分利用已有模型的能力，通过工程化集成实现新的功能组合。

与前两篇论文的联系：PhysGen关注的是如何让生成的视频符合物理规律，TopoGen关注的是如何让生成的三维形状具有正确的拓扑结构，RBF-PoroNet关注的是如何自动优化结构使其满足物理性能需求。三者从不同角度体现了"将物理/数学先验引入生成式AI"的研究趋势。

---

## 四、补充学习材料

### 4.1 小红书参考链接

**链接：** https://www.xiaohongshu.com/explore/69fc9a47000000003802382d

**说明：** 由于该平台对非移动端访问存在IP安全限制，当前无法通过网页直接获取内容。后续将在移动端查看并补充笔记。根据链接ID和上下文推断，该内容可能涉及 PhysGen 或物理驱动视频生成相关的技术介绍/复现分享。待确认后补充。

---

## 五、学习总结与下阶段计划

### 5.1 本阶段收获

通过本阶段的学习，我对以下三个前沿方向有了系统性的认识：

**拓扑感知的三维生成（TopoGen）：** 理解了如何利用代数拓扑工具（持久同调）为三维生成模型提供全局结构性约束，这是超越局部几何特征的一种高层次建模方式。

**可微隐式优化框架（RBF-PoroNet）：** 学习了如何将神经网络的可微性优势引入传统结构优化问题，实现端到端的自动化设计流程，以及SA-RBFNN作为隐式曲面表示的创新思路。

**物理驱动的视频生成（PhysGen）：** 掌握了一种将物理仿真引擎与视觉基础模型相结合的系统设计范式，体会到 "training-free pipeline" 在大模型时代的工程价值。

### 5.2 三篇工作的关联与启示

三篇工作虽然分属不同子领域（计算机图形学、增材制造、计算机视觉），但共享一个核心思想：**将严格的数学/物理先验注入数据驱动的生成模型**。具体而言：

- TopoGen：拓扑代数 → 三维形状生成
- RBF-PoroNet：连续可微物理场 → 结构优化
- PhysGen：刚体动力学 → 视频生成

这种 "先验知识 + 深度学习" 的融合范式，正在成为AI for Science 和 AI for Engineering 的主流方法论。

### 5.3 下阶段工作计划

1. 深入阅读 TopoGen 的完整论文（尝试通过机构图书馆获取 CGF 全文），重点关注拓扑损失函数的可微化实现细节；
2. 复现 RBF-PoroNet 的代码（如有开源），在简单算例上验证SA-RBFNN的优化效果；
3. 在 PhysGen 基础上尝试扩展到更复杂的场景（如多物体交互、非平面运动），分析当前框架的能力边界；
4. 补充小红书链接中的学习内容；
5. 探索三篇工作的交叉点：是否可以构建一个"拓扑感知+物理驱动"的统一三维生成框架。

---

## 六、参考文献

[1] Hu J, Fei B, Xu B, et al. TopoGen: Topology-Aware 3D Generation with Persistence Points[J]. Computer Graphics Forum, 2025. DOI: 10.1111/cgf.70257

[2] Du H, Wang S, et al. RBF-PoroNet: A Differentiable Implicit Framework for Automated Optimization of Porous Structures via a Self-Adaptive RBF Neural Network[J]. Additive Manufacturing, 2025. DOI: 10.1016/j.addma.2026.104XXX

[3] Liu S, Ren Z, Gupta S, Wang S. PhysGen: Rigid-Body Physics-Grounded Image-to-Video Generation[C]. ECCV 2024. arXiv: 2409.18964

---

*本汇报由张宏浩撰写，如有疏漏之处请导师指正。*
