# 统计学全方法考点手册

> **覆盖范围：** 线性回归、分类、正则化、树模型、逻辑回归，以及近年新增高频的 **KNN / SVM / 样条 / GAM / PCA / 聚类 / Bootstrap / 代码题套路**。  
> **使用方式：** 先看“考试题型与套路总览”，再按模块复习；考前优先刷带 ⭐⭐ / ⭐⭐⭐ 的表格、模板和陷阱。  
> **标注说明：** ⭐高频考点 ⭐⭐必考 ⭐⭐⭐极高频陷阱

---

## 目录

1. [考试题型与套路总览](#考试题型与套路总览)
2. [线性回归模块](#一线性回归模块)
3. [分类方法模块](#二分类方法模块)
4. [正则化与变量选择模块](#三正则化与变量选择模块)
5. [树模型与集成学习模块](#四树模型与集成学习模块)
6. [逻辑斯蒂回归专项](#五逻辑斯蒂回归专项)
7. [距离方法与支持向量机模块](#六距离方法与支持向量机模块)
8. [非线性回归与样条模块](#七非线性回归与样条模块)
9. [降维与聚类模块](#八降维与聚类模块)
10. [重抽样与代码题模块](#九重抽样与代码题模块)
11. [例题串讲（完整版）](#十例题串讲完整版)

---

# 考试题型与套路总览

## 0.1 近年卷面最常见的三类题 ⭐⭐⭐

| 题型 | 常见问法 | 拿分抓手 |
|------|---------|---------|
| **选择/判断题** | 概念辨析、参数极端值、方法对比、R函数识别 | 先认关键词：$\lambda$、$C$、$K$、VIF、AIC/BIC、Gini、OOB |
| **简答/表格题** | 解释回归输出、解释系数、比较模型、说明参数变化 | 先看**p值/显著性**，再看方向，最后补一句"控制其他变量不变" |
| **计算/编程题** | 混淆矩阵推广、逻辑回归概率、K均值迭代、R代码解读 | 一定**先写公式再代数值**，代码题先判断模型再说参数作用 |

## 0.2 近年最容易反复考的套路

| 套路 | 高频问法 | 标准反应 |
|------|---------|---------|
| **表格输出题** | "变量是否显著？"、"解释系数含义" | p值判显著，系数符号判方向，多元模型必须写"其他变量保持不变" |
| **参数影响题** | "$\lambda$ 增大时会怎样？"、"$C$ 变大时边界如何变化？" | 从**偏差↑/方差↓**或**模型更平滑/更复杂**两个角度答 |
| **方法对比题** | "LDA vs QDA"、"Ridge vs LASSO"、"Bagging vs Boosting" | 固定四个维度：假设、边界/约束、优缺点、适用场景 |
| **逻辑回归题** | "算违约概率"、"解释OR"、"为什么系数变号？" | 先写log-odds，再转概率；变号优先想到**混淆/辛普森悖论** |
| **混淆矩阵题** | "错误率是多少？"、"推广到更大测试集会错判多少人？" | 先写矩阵名称，再写TPR/FPR公式，再乘新样本规模 |
| **代码题** | "这段代码在做什么？" | 按**数据切分 → 模型 → 调参 → 评价指标 → 结论**五步答 |

## 0.3 五个通用答题模板

1. **回归输出题：** 先说 p值是否小于 0.05，再说系数正负和大小，最后补一句现实解释。
2. **概率计算题：** 先写线性预测子，再写 log-odds，最后转成概率 $p=\frac{e^\eta}{1+e^\eta}$。
3. **参数变化题：** 先说参数从小到大时模型是更灵活还是更平滑，再说偏差/方差方向。
4. **对比题：** 先写共同点，再写两条本质区别，最后给一句适用场景。
5. **代码题：** 不要一上来抄代码，先点名这是哪一类模型，再解释关键函数和输出指标。

> **高频数据集提醒：** `Advertising` 常考线性回归与交互项，`Default/Credit` 常考逻辑回归、混淆变量与概率计算，`Hitters` 常考正则化、Boosting 与代码题。

---

# 一、线性回归模块

## 1.1 核心符号速查

| 符号 | 名称 | 含义 |
|------|------|------|
| $y_i$ | 观测值 | 第 $i$ 个样本的真实值 |
| $\hat{y}_i$ | 拟合值 | 模型对第 $i$ 个样本的预测值 |
| $\bar{y}$ | 样本均值 | 所有 $y_i$ 的算术平均 |
| $e_i = y_i - \hat{y}_i$ | **残差** | 可观测；OLS保证 $\sum e_i = 0$ |
| $\varepsilon_i$ | **误差项** | 不可观测的理论量 |
| $\hat{\beta}$ | 估计系数 | 由样本数据估计出的 $\beta$ |

**残差 vs 误差项（必考陷阱）：**

| | 残差 $e_i$ | 误差项 $\varepsilon_i$ |
|--|-----------|----------------------|
| 是否可观测 | ✅ 可以计算 | ❌ 不可观测 |
| 均值 | $\sum e_i = 0$（OLS保证）| $E(\varepsilon_i) = 0$（假设）|

---

## 1.2 平方和分解（必须背熟）⭐⭐⭐

$$\underbrace{TSS}_{\text{总变异}} = \underbrace{ESS}_{\text{已解释}} + \underbrace{RSS}_{\text{未解释（残差）}}$$

| 名称 | 公式 | 自由度 |
|------|------|--------|
| **TSS**（总）| $\sum(y_i-\bar{y})^2$ | $n-1$ |
| **ESS**（回归）| $\sum(\hat{y}_i-\bar{y})^2$ | $p$ |
| **RSS**（残差）| $\sum(y_i-\hat{y}_i)^2$ | $n-p-1$ |

> ⚠️ **命名陷阱：** 不同教材对 ESS/SSR/SSE/RSS 缩写不一致，考试以题目定义为准，记住 **"总 = 已解释 + 未解释"**。

---

## 1.3 一元线性回归

### 模型公式

$$Y_i = \beta_0 + \beta_1 X_i + \varepsilon_i \qquad \hat{Y}_i = \hat{\beta}_0 + \hat{\beta}_1 X_i$$

$$\hat{\beta}_1 = \frac{S_{xy}}{S_{xx}} = \frac{\sum(x_i-\bar{x})(y_i-\bar{y})}{\sum(x_i-\bar{x})^2}, \qquad \hat{\beta}_0 = \bar{y} - \hat{\beta}_1\bar{x}$$

### 如何看回归输出表格 ⭐⭐

```
              Estimate  Std.Error  t value  Pr(>|t|)
(Intercept)    2.500     0.800      3.125    0.0052 **
x              1.300     0.250      5.200    0.0001 ***
---
Residual standard error: 1.42 on 28 degrees of freedom
Multiple R-squared: 0.765,  Adjusted R-squared: 0.757
F-statistic: 89.3 on 1 and 28 DF,  p-value: <0.0001
```

| 列 | 符号 | 含义 | 考试关注点 |
|---|------|------|-----------|
| **Estimate** | $\hat{\beta}$ | 系数估计值 | 方向（正/负）和大小 |
| **Std.Error** | $SE(\hat{\beta})$ | 标准误 | 越小越精确 |
| **t value** | $\hat{\beta}/SE$ | t统计量 | $|t|>2$ 通常显著 |
| **Pr(>\|t\|)** | p值 | 双尾检验 | **<0.05 则显著** |

**显著性标记：**

| 星号 | p值范围 | 含义 |
|------|---------|------|
| `***` | $<0.001$ | 极显著 |
| `**` | $<0.01$ | 高度显著 |
| `*` | $<0.05$ | 显著 |
| （空）| $\geq 0.1$ | 不显著 |

> **速读口诀：** 估计看方向，标误看精度，t值看显著，p值做结论

### OLS 假设（LINE）⭐

| 假设 | 内容 | 违反后果 |
|------|------|---------|
| **L**inear | $y$ 与 $x$ 之间线性关系 | 系数估计有偏 |
| **I**ndependent | 各观测相互独立 | 标准误估计失效 |
| **N**ormal | 误差项服从正态分布 | 小样本推断失效 |
| **E**qual variance | 误差项方差恒定（同方差）| 标准误估计失效 |

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 斜率解释 | "解释系数1.3含义" | "$x$ 每增加1单位，$y$ 平均增加1.3单位" |
| 截距解释 | "截距有实际意义吗？" | 若 $x=0$ 不在数据范围内，截距**无实际意义** |
| 系数显著性 | "是否显著影响y？" | 看p值，$<0.05$ 则拒绝 $H_0:\beta_1=0$，认为显著 |
| 预测 | "当x=5时，y的预测值？" | $\hat{y} = \hat{\beta}_0 + \hat{\beta}_1 \times 5$ |

### 必考公式速查

$$SE(\hat{\beta}_1) = \sqrt{\frac{MSE}{S_{xx}}}, \qquad t = \frac{\hat{\beta}_1}{SE(\hat{\beta}_1)} \sim t_{n-2}$$

$$R^2 = 1 - \frac{RSS}{TSS}, \qquad MSE = \frac{RSS}{n-2}$$

$$\text{CI（置信区间）for }\hat{y}^*: \quad \hat{y}^* \pm t_{\alpha/2}\sqrt{MSE\left(\frac{1}{n}+\frac{(x^*-\bar{x})^2}{S_{xx}}\right)}$$

$$\text{PI（预测区间）for }Y_{new}: \quad \hat{y}^* \pm t_{\alpha/2}\sqrt{MSE\left(\mathbf{1}+\frac{1}{n}+\frac{(x^*-\bar{x})^2}{S_{xx}}\right)}$$

---

## 1.4 多元线性回归

### 模型公式

$$Y = \beta_0 + \beta_1X_1 + \cdots + \beta_pX_p + \varepsilon$$

$$\hat{\boldsymbol{\beta}} = (X^TX)^{-1}X^Ty$$

### 偏效应（最重要考点）⭐⭐⭐

> **$\hat{\beta}_j$ 的含义：在其他所有变量保持不变的条件下，$X_j$ 每增加1单位，$Y$ 平均变化 $\hat{\beta}_j$ 单位。**

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 偏效应（最重要）| "解释$\hat\beta_2=-0.32$" | "在$x_1,x_3$**保持不变**的条件下，$x_2$每增加1单位，$y$平均减少0.32单位" |
| 整体显著性 | "模型整体是否显著？" | 看F统计量p值，$<0.05$则整体显著 |
| 加变量R²必涨 | "为何用Adj.R²？" | 加入无关变量时R²不减但Adj.R²下降，跨参数量比较必须用Adj.R² |
| F与t的关系 | "F显著但t不显著，矛盾吗？" | 不矛盾！共线性时常出现，F检验整体，t检验个别 |

### F统计量与ANOVA表

$$F = \frac{ESS/p}{RSS/(n-p-1)} \sim F_{p,n-p-1}$$

| 来源 | SS | df | MS | F |
|------|----|----|----|----|
| 回归 | ESS | $p$ | $MSR=ESS/p$ | $F=MSR/MSE$ |
| 残差 | RSS | $n-p-1$ | $MSE=RSS/(n-p-1)$ | |
| 总计 | TSS | $n-1$ | | |

$$\bar{R}^2 = 1-(1-R^2)\frac{n-1}{n-p-1}, \qquad F_{1,n-p-1} = t_{n-p-1}^2 \text{（一元时等价）}$$

---

## 1.5 置信区间 vs 预测区间 ⭐⭐⭐

| | 置信区间（CI）| 预测区间（PI）|
|--|-------------|-------------|
| **估计对象** | 均值 $E(Y\|X=x^*)$ | 单个新观测 $Y_{new}$ |
| **宽度** | 较**窄** | 较**宽**（多1项）|
| **$n\to\infty$时** | 宽度→0 | 宽度→$2\times1.96\sqrt{MSE}$（**不为零！**）|
| **最窄处** | $x^*=\bar x$ | $x^*=\bar x$ |

> **记忆口诀：** "估均值用CI（窄），预测个体用PI（宽）"

---

## 1.6 多重共线性与VIF ⭐⭐

$$VIF_j = \frac{1}{1-R_j^2}$$

| VIF | 共线性程度 | 建议 |
|-----|---------|------|
| =1 | 无 | 正常 |
| 1~5 | 轻度 | 可接受 |
| 5~10 | 中度 | 需关注 |
| **≥10** | **严重** | 需处理 |

**最典型症状：** F检验显著，但各t检验不显著

**多重共线性不影响：** OLS无偏性、$R^2$和F检验、样本内预测

**多重共线性影响：** 系数标准误膨胀、t检验可靠性、系数解释

---

## 1.7 交互项 ⭐⭐

$$Y = \beta_0 + \beta_1X_1 + \beta_2X_2 + \beta_3X_1X_2 + \varepsilon$$

$$\frac{\partial E(Y)}{\partial X_1} = \beta_1 + \beta_3X_2 \quad \text{（$X_1$的边际效应随$X_2$变化）}$$

**考试问法与答法：**

| 问法 | 答法 |
|------|------|
| "radio增加1单位，TV的效应如何变化？" | "TV的边际效应增加$\beta_3$单位" |
| "能否去掉交互项？" | "看p值，若显著则不能去掉" |
| "如何解释主效应$\beta_1$？" | "$\beta_1$是当$X_2=0$时$X_1$的效应" |

> ⚠️ **层次原则（必考陷阱）：** 交互项显著时，**即使主效应p>0.05也不能删除主效应！**

---

## 1.8 线性回归高频陷阱总表

| 编号 | 陷阱 | 正确理解 |
|------|------|---------|
| T1 | 相关≠因果 | 回归只能说明线性关联，不能说明因果 |
| T2 | 外推陷阱 | 数据范围外预测不可靠，需明确说明 |
| T3 | 截距强行解释 | $x=0$不在范围内时截距无实际意义 |
| T4 | 残差vs误差 | 残差可观测，误差项不可观测 |
| T5 | 自由度错误 | 一元$n-2$，多元$n-p-1$ |
| T6 | 单尾vs双尾 | "是否有影响"→双尾；"是否正向影响"→单尾 |
| T7 | R²增加≠更好 | 跨参数量比较用Adj.R² |
| T8 | F显著则t显著 | 共线性时F显著但t可不显著 |
| T9 | CI=PI | CI估均值（窄），PI预测个体（宽）|
| T10 | 共线性使OLS有偏 | OLS仍无偏，但方差膨胀 |
| T11 | VIF阈值 | $<5$可接受，$\geq10$严重 |
| T12 | 交互项层次原则 | 有交互项时**必须保留**主效应 |

---

# 二、分类方法模块

## 2.1 朴素贝叶斯分类器（Naive Bayes）

### 核心公式

**贝叶斯定理：**

$$\underbrace{P(Y=k|X=x)}_{\text{后验 Posterior}} = \frac{\underbrace{P(X=x|Y=k)}_{\text{似然 Likelihood}} \cdot \underbrace{P(Y=k)}_{\text{先验 Prior}}}{\underbrace{P(X=x)}_{\text{证据 Evidence}}}$$

**朴素假设（条件独立）：**

$$P(X=x|Y=k) = \prod_{j=1}^p P(X_j=x_j|Y=k)$$

**分类决策（MAP）：**

$$\hat{Y} = \arg\max_k \left[\log\pi_k + \sum_j \log P(X_j=x_j|Y=k)\right]$$

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| "朴素"的含义 | "'朴素'体现在哪？" | 在给定$Y$的条件下，假设所有特征**条件独立** |
| 生成vs判别 | "NB是哪类模型？" | **生成模型**，对$P(X\|Y)$和$P(Y)$分别建模 |
| 零概率问题 | "如何处理未见特征值？" | **拉普拉斯平滑**（加$\alpha$），避免概率为零 |
| 为何取对数 | "为何实际计算取对数？" | 避免连乘数值下溢，且不改变$\arg\max$结果 |
| 高维优势 | "NB何时效果好？" | 特征近似条件独立时（如文本），高维，小样本 |

---

## 2.2 线性判别分析（LDA）

### 核心假设

$$X|Y=k \sim \mathcal{N}(\mu_k, \Sigma) \quad \text{（所有类共享同一协方差矩阵}\Sigma\text{）}$$

### 判别函数

$$\boxed{\delta_k(x) = x^T\Sigma^{-1}\mu_k - \frac{1}{2}\mu_k^T\Sigma^{-1}\mu_k + \log\pi_k}$$

$$\hat{Y} = \arg\max_k \delta_k(x)$$

**为什么是线性：** 各类共享$\Sigma$，二次项$x^T\Sigma^{-1}x$与$k$无关，比较时相消，剩余项关于$x$是**线性**的。

### 参数估计

$$\hat\pi_k = \frac{n_k}{n}, \quad \hat\mu_k = \frac{1}{n_k}\sum_{i:y_i=k}x_i, \quad \hat\Sigma = \frac{1}{n-K}\sum_k\sum_{i:y_i=k}(x_i-\hat\mu_k)(x_i-\hat\mu_k)^T$$

> ⚠️ 合并协方差矩阵分母为 $n-K$，不是 $n$ 或 $n_k$！

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 线性边界的条件 | "LDA为何产生线性边界？" | 各类共享$\Sigma$，二次项与类别无关相消 |
| 合并协方差 | "LDA如何估计协方差矩阵？" | 对所有类内散度加权平均，分母$n-K$ |
| 先验的作用 | "先验不等时对LDA有何影响？" | $\log\pi_k$项使决策边界向先验小的类偏移 |
| LDA降维 | "LDA如何用于降维？" | 最多降到$\min(K-1,p)$维，最大化类间/类内散度比 |

---

## 2.3 二次判别分析（QDA）

### 核心假设

$$X|Y=k \sim \mathcal{N}(\mu_k, \Sigma_k) \quad \text{（各类有各自的协方差矩阵}\Sigma_k\text{）}$$

### 判别函数

$$\boxed{\delta_k^Q(x) = -\frac{1}{2}\log|\Sigma_k| - \frac{1}{2}(x-\mu_k)^T\Sigma_k^{-1}(x-\mu_k) + \log\pi_k}$$

**为什么是二次：** 各类$\Sigma_k$不同，展开后含$x^T\Sigma_k^{-1}x$项，该项随$k$变化，**不能相消**，故为二次。

### 参数估计

$$\hat\Sigma_k = \frac{1}{n_k-1}\sum_{i:y_i=k}(x_i-\hat\mu_k)(x_i-\hat\mu_k)^T \quad \text{（各类单独估计，分母}n_k-1\text{）}$$

> ⚠️ QDA分母是$n_k-1$（各类单独估计），LDA分母是$n-K$（合并估计）

---

## 2.4 LDA vs QDA vs 朴素贝叶斯 vs 逻辑回归 终极对比 ⭐⭐⭐

| 比较维度 | LDA | QDA | 朴素贝叶斯 | 逻辑回归 |
|---------|-----|-----|----------|---------|
| 模型类型 | 生成 | 生成 | 生成 | **判别** |
| 分布假设 | 多元正态，共享$\Sigma$ | 多元正态，各类$\Sigma_k$ | 各特征独立正态 | **无** |
| 特征独立假设 | ❌ | ❌ | **✅** | ❌ |
| 等协方差假设 | **✅** | ❌ | ❌ | ❌ |
| 决策边界 | **线性** | **二次** | 线性或二次 | 线性 |
| 协方差参数数 | $p(p+1)/2$（一个）| $Kp(p+1)/2$（K个）| $2pK$（对角）| 无 |
| 偏差 | 高（若假设错）| 低（假设弱）| 中 | 中 |
| 方差 | 低 | 高 | 低 | 中 |
| 小样本 | ✅好 | ❌差 | ✅好 | 中 |
| 高维数据 | ❌（$\Sigma$奇异）| ❌❌ | **✅** | ✅（可正则化）|
| 过拟合风险 | 低 | 高 | 低 | 低~中 |
| 可解释性 | 高 | 中 | 高 | 高 |

**选择口诀：**
- 各类协方差相似 + 小样本 → **LDA**
- 各类协方差差异大 + 大样本 → **QDA**
- 高维 + 特征近似独立（如文本）→ **朴素贝叶斯**
- 无分布假设 / 高维 → **逻辑回归**

---

## 2.5 混淆矩阵与评估指标 ⭐⭐⭐

| 真实\预测 | 预测负（$\hat Y=0$）| 预测正（$\hat Y=1$）| 总计 |
|--|---------------------|---------------------|------|
| **实际负（$Y=0$）** | **TN**（真阴性）| **FP**（假阳性/误报）| $N=TN+FP$ |
| **实际正（$Y=1$）** | **FN**（假阴性/漏报）| **TP**（真阳性）| $P=FN+TP$ |
| **总计** | $N^*=TN+FN$ | $P^*=TP+FP$ | $n=P+N$ |

> **课堂常用记号：** $P/N$ 表示真实正负样本数，$P^*/N^*$ 表示预测为正/负的样本数。

$$TPR = \frac{TP}{P} = \frac{TP}{TP+FN}, \qquad FPR = \frac{FP}{N} = \frac{FP}{FP+TN}$$

$$\text{Precision} = \frac{TP}{P^*} = \frac{TP}{TP+FP}, \qquad \text{Specificity} = \frac{TN}{N} = \frac{TN}{TN+FP}$$

$$\text{NPV} = \frac{TN}{N^*} = \frac{TN}{TN+FN}, \qquad \text{Accuracy} = \frac{TP+TN}{TP+TN+FP+FN}$$

$$F1 = \frac{2\cdot \text{Precision}\cdot \text{Recall}}{\text{Precision}+\text{Recall}}, \qquad \text{FDR} = \frac{FP}{P^*}$$

| 指标 | 公式 | 同义词/关注点 |
|------|------|--------------|
| **Recall（TPR）** | $TP/P$ | 灵敏度（Sensitivity）、召回率；漏报代价高时重点看 |
| **Specificity** | $TN/N$ | 特异度；和 FPR 对应 |
| **Precision** | $TP/P^*$ | 精确率；误报代价高时重点看 |
| **NPV** | $TN/N^*$ | 阴性预测值 |
| **F1** | $2PR/(P+R)$ | 兼顾 Precision 与 Recall |
| **AUC** | ROC曲线下面积 | 模型综合评估，越接近1越好 |
| **FDR** | $FP/P^*$ | 错误发现率 |

### 混淆矩阵大题模板（近年高频）⭐⭐⭐

**第一步：先写名称与四格含义**

> 这是**混淆矩阵（confusion matrix）**。按课堂常用写法：**行是真实分类，列是预测分类；第一行是真实负类，第二行是真实正类**。因此第一行是 `TN / FP`，第二行是 `FN / TP`。

**第二步：基础指标直接写公式**

$$P = TP+FN, \qquad N = TN+FP, \qquad P^* = TP+FP, \qquad N^* = TN+FN$$

$$\text{Error Rate} = \frac{FP+FN}{TP+FP+FN+TN}$$

$$TPR = \frac{TP}{P}, \qquad FPR = \frac{FP}{N}, \qquad \text{Precision} = \frac{TP}{P^*}$$

$$\text{Specificity} = \frac{TN}{N}, \qquad \text{NPV} = \frac{TN}{N^*}$$

**第三步：推广到新的测试集规模**

若新的测试集里有 $N_1$ 个真实正例、$N_0$ 个真实负例，则：

$$\text{成功检出的正例数} = TPR \times N_1$$

$$\text{漏报的正例数} = (1-TPR) \times N_1$$

$$\text{被错判为正例的负例数} = FPR \times N_0$$

$$\text{预测为正的总人数} = TPR\times N_1 + FPR\times N_0$$

**考试拿分顺序：**
- 先写矩阵名称
- 再写公式
- 最后代入数字
- 推广题优先想到 **TPR 乘真实正例人数，FPR 乘真实负例人数**

---

## 2.6 分类方法高频陷阱

| 陷阱 | 正确理解 |
|------|---------|
| LDA一定有线性边界 | 依赖正态+等协方差假设，假设不成立时线性边界仍存在但不最优 |
| QDA总比LDA好 | 小样本时QDA方差大，可能不如LDA |
| NB是LDA的特例 | 部分情形下是，一般不成立 |
| $\log\|\Sigma_k\|$项不重要 | QDA中**必须包含**该项，LDA可忽略（因共享$\Sigma$）|
| Precision和Recall定义 | P分母是预测为正的总数，R分母是实际为正的总数 |
| 类别不平衡时用Accuracy | 不平衡时Accuracy失效，应用F1或AUC |

---

# 三、正则化与变量选择模块

## 3.1 偏差-方差权衡（Bias-Variance Tradeoff）⭐⭐⭐

$$\boxed{E[(y_0-\hat{f}(x_0))^2] = \underbrace{[\text{Bias}(\hat{f}(x_0))]^2}_{\text{偏差}^2} + \underbrace{\text{Var}(\hat{f}(x_0))}_{\text{方差}} + \underbrace{\sigma^2}_{\text{不可约误差}}}$$

| $\lambda$变化 | 偏差 | 方差 | 训练误差 | 测试误差 |
|------------|------|------|---------|---------|
| $\lambda$增大 | ↑ | ↓ | ↑ | 先↓后↑（**U形**）|
| $\lambda=0$ | 最小 | 最大 | 最小 | 可能高 |
| $\lambda\to\infty$ | 最大 | 最小 | 最大 | 最大（欠拟合）|

**过拟合 vs 欠拟合：**

| | 欠拟合 | 过拟合 |
|--|--------|--------|
| 偏差 | 高 | 低 |
| 方差 | 低 | 高 |
| 训练误差 | 高 | 低 |
| 测试误差 | 高 | 高 |
| 解决方案 | 增加复杂度 | 正则化 |

---

## 3.2 岭回归（Ridge / L2正则化）⭐⭐⭐

### 目标函数

$$\boxed{\hat\beta^{ridge} = \arg\min_\beta\left\{\underbrace{\sum_i\left(y_i-\beta_0-\sum_j\beta_jx_{ij}\right)^2}_{\text{RSS}} + \underbrace{\lambda\sum_{j=1}^p\beta_j^2}_{\text{L2惩罚}}\right\}}$$

解析解：$\hat\beta^{ridge} = (X^TX+\lambda I)^{-1}X^Ty$

### 关键性质

| 性质 | 内容 |
|------|------|
| 系数能否为零 | **不能**（只趋近于零）|
| 变量选择能力 | **无** |
| $\lambda=0$ | 退化为OLS |
| $\lambda\to\infty$ | 所有系数趋于0 |
| 解决多重共线性 | $\lambda I$使矩阵正定，系数估计稳定 |
| 估计性质 | **有偏**（引入偏差换取方差降低）|

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 惩罚项形式 | "写出岭回归目标函数" | RSS + $\lambda\sum\beta_j^2$，**截距不惩罚** |
| 能否变量选择 | "岭回归能将系数精确压缩为0吗？" | **不能**，保留所有变量 |
| 标准化必要性 | "使用前需要什么预处理？" | **必须标准化**（量纲不同则惩罚不公平）|

---

## 3.3 LASSO回归（L1正则化）⭐⭐⭐

### 目标函数

$$\boxed{\hat\beta^{lasso} = \arg\min_\beta\left\{\text{RSS} + \lambda\sum_{j=1}^p|\beta_j|\right\}}$$

### 关键性质

| 性质 | 内容 |
|------|------|
| 系数能否为零 | **能**（精确等于零）|
| 变量选择能力 | **有**（稀疏解）|
| 约束域形状 | **菱形**（有尖角→解易落在坐标轴上）|
| 系数路径 | 折线，系数**逐个精确归零** |
| 解析解 | **无**（需迭代算法）|

**LASSO产生稀疏解的几何直觉：**

```
岭回归（圆形约束域）      LASSO（菱形约束域）
      β₂                      β₂
       |                      /|\
  _____|_____               /  |  \
 /     |     \            /    |    \
|      |      |          /     |     \
-------|-------β₁        -------|-------β₁
|      |      |          \     |     /
 \_____._____/            \    |    /
                           \   |   /
                            \  |  /
                             顶点！→ 系数=0
```

### 软阈值（正交设计时的解析形式）

$$\hat\beta_j^{lasso} = \text{sign}(\hat\beta_j^{OLS})\cdot\max\left(|\hat\beta_j^{OLS}|-\frac\lambda2, 0\right)$$

当 $|\hat\beta_j^{OLS}|<\lambda/2$ 时，系数**精确归零**。

---

## 3.4 岭回归 vs LASSO 核心对比 ⭐⭐⭐

| 对比维度 | 岭回归（Ridge）| LASSO |
|---------|-------------|-------|
| **惩罚项** | $\lambda\sum\beta_j^2$（L2）| $\lambda\sum|\beta_j|$（L1）|
| **约束域形状** | 圆形（光滑）| **菱形（有尖角）**|
| **系数能否为零** | **不能** | **能** |
| **稀疏性** | 无 | **有** |
| **变量选择** | **不能** | **能** |
| **系数路径** | 平滑曲线 | 折线，逐个归零 |
| **解析解** | 有 | 无（需迭代）|
| **适用场景** | 所有变量均有贡献，多重共线性 | **真实模型稀疏**，需变量选择 |
| **高相关变量处理** | 系数均匀收缩，**稳定** | 随机选一个，**不稳定** |
| **$p>n$时** | 可用 | 最多选$n$个变量 |

**选择建议：**
- 真实模型稀疏（少数变量重要）→ **LASSO**
- 多数变量均有贡献，多重共线性 → **Ridge**
- 介于两者之间 → **Elastic Net**（$\alpha\|\beta\|_1 + (1-\alpha)\|\beta\|_2^2$）

---

## 3.5 变量选择方法 ⭐⭐⭐

### 最优子集选择

**总模型数：$2^p$**

| $p$ | $2^p$ | 可行性 |
|-----|-------|--------|
| 10 | 1,024 | 可行 |
| 20 | ~100万 | 勉强 |
| 30 | ~10亿 | **不可行** |

### 向前逐步选择

- 从空模型开始，每步加一个变量（贪心）
- **总模型数：$1+p(p+1)/2$**
- $p>n$时**仍可用**
- 已加入的变量**不会被删除**

### 向后逐步选择

- 从全模型开始，每步删一个变量（贪心）
- **总模型数：$1+p(p+1)/2$**
- **必须$n>p$**（第一步需拟合全模型）
- 已删除的变量**不会被加回**

### 三种方法对比

| | 最优子集 | 向前逐步 | 向后逐步 |
|--|---------|---------|---------|
| **模型总数** | $2^p$ | $1+p(p+1)/2$ | $1+p(p+1)/2$ |
| **找到全局最优** | **保证**（训练集意义上）| 不保证 | 不保证 |
| **$p>n$时** | 不可用 | **可用** | **不可用** |
| **$p>30$时** | 不可行 | 可行 | 可行（需$n>p$）|
| **算法类型** | 精确 | 贪心 | 贪心 |

---

## 3.6 模型选择标准 ⭐⭐⭐

> **先记结论：** 这些标准本质上都在做同一件事——在“拟合得好”和“别太复杂”之间找平衡。  
> **考试最爱考：** `Cp / AIC / BIC` 都是**越小越好**，`Adj.R^2` 是**越大越好**；其中 **BIC 最保守，最容易选出最简单模型**。

| 标准 | 方向 | 你真正要记的点 |
|------|------|----------------|
| **$C_p$** | **越小越好** | 线性回归里可看成和AIC一个路子 |
| **AIC** | **越小越好** | 通用，惩罚中等 |
| **BIC** | **越小越好** | 惩罚最重，最偏向简单模型 |
| **Adj.$R^2$** | **越大越好** | 最直观，适合解释“加变量不一定更好” |

**一句话秒选：**
- 问“哪个标准最保守、最容易选最少变量” → **BIC**
- 问“哪个标准越大越好” → **Adj.$R^2$**
- 问“为什么不能只看 $R^2$” → 因为加变量时 $R^2$ 几乎只涨不跌

> **关键：** BIC 的惩罚最重，所以它通常会选出**变量更少、更简单**的模型。

---

## 3.7 调整参数λ的选择：K折交叉验证 ⭐⭐⭐

> **一句话：** 交叉验证不是看谁把训练集拟合得最好，而是看谁在“没见过的数据”上更稳。

**正确流程（只背这4步）：**

```
1. 把数据随机分成 K 折（常用 K=5 或 10）
2. 固定一个候选 λ，用 K-1 折训练、1 折验证
3. 每一折都轮一遍，取平均验证误差 → CV(λ)
4. 选 CV 误差最小的 λ，再用全部训练数据重拟合
```

**考试最爱考的两个量：**

| 名称 | 含义 | 该怎么记 |
|------|------|---------|
| `lambda.min` | CV误差最小的 $\lambda$ | **最准** |
| `lambda.1se` | 在“最小误差 + 1个SE”范围内取最大的 $\lambda$ | **更简单、更稳** |

> **One-SE Rule：** 不追求“最小误差那一个”，而是在误差几乎差不多的前提下，选**更简单**的模型。

---

## 3.8 正则化高频陷阱

| 陷阱 | 正确认识 |
|------|---------|
| 截距参与惩罚 | **截距不参与惩罚**，只对$\beta_1\ldots\beta_p$惩罚 |
| 不标准化 | **必须标准化**，否则量纲不同导致惩罚不公平 |
| 训练误差选模型 | **必须用CV/AIC/BIC**，训练误差随复杂度单调减小 |
| LASSO总比Ridge好 | 取决于真实模型是否稀疏，无免费午餐 |
| 向后逐步不需$n>p$ | **必须$n>p$**，否则全模型OLS无唯一解 |
| CV中先选变量再验证 | **数据泄露！** 必须每折内重新运行变量选择 |
| BIC=AIC | BIC惩罚更重（$\ln n>2$），选出的模型更简单 |

---

# 四、树模型与集成学习模块

## 4.1 回归树

### 核心思想

> **一句话：** 回归树就是不断问“某个变量是否大于某个切分点”，把数据切成几块，最后在叶节点里用**均值**做预测。

**递归二分（Recursive Binary Splitting）：** 每次选特征$j$和切分点$s$，最小化：

$$\text{RSS} = \sum_{i\in R_1}(y_i-\bar y_{R_1})^2 + \sum_{i\in R_2}(y_i-\bar y_{R_2})^2$$

**预测：** 叶节点内所有训练样本的**均值**。

**贪心性：** 每一步只顾当前切得最好，所以是**局部最优**，不保证全局最优。

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 分裂准则 | "写出回归树的分裂准则" | 最小化分裂后两边的 RSS 之和 |
| 预测方式 | "如何对新样本预测？" | 顺着树往下走到叶节点，返回该叶子的均值 |
| 最大缺点 | "回归树的局限性？" | 高方差、不稳定；训练集稍变，树可能变化很大 |

---

## 4.2 分类树

### 核心思想

> **一句话：** 分类树就是不断切分，让每个叶节点里的样本尽量“同类”，最后用该叶节点里占比最高的类别做预测。

预测规则：$\hat{y} = \arg\max_k \hat{p}_{mk}$（叶节点中占比最高的类别）

### 分裂准则详解 ⭐⭐⭐

#### Gini不纯度

> **通俗理解：** Gini 越小，节点越纯；如果一个节点里几乎全是一类，那么 Gini 就会很接近 0。

$$\boxed{G_m = 1 - \sum_{k=1}^K \hat{p}_{mk}^2 = \sum_{k=1}^K\hat p_{mk}(1-\hat p_{mk})}$$

**加权Gini（分裂后）：**

$$G_{\text{split}} = \frac{n_L}{n}G_L + \frac{n_R}{n}G_R$$

**Gini增益：** $\Delta G = G_{\text{parent}} - G_{\text{split}}$，选使增益**最大**的分裂。

**算题固定四步：**
1. 先算父节点 Gini
2. 再算左右子节点 Gini
3. 按样本数做加权平均
4. 用“父节点 - 分裂后”算 Gini 增益

**计算例子：** 节点10样本：6类A，4类B

$$G = 1-(0.6^2+0.4^2) = 0.48$$

#### 信息熵（Entropy）

> **通俗理解：** 熵也是衡量“乱不乱”，越小越纯，和 Gini 的思路一样。

$$\boxed{H_m = -\sum_{k=1}^K \hat{p}_{mk}\log_2\hat{p}_{mk}}$$

**计算例子（同上）：**

$$H = -0.6\log_2(0.6)-0.4\log_2(0.4) = 0.971 \text{ 比特}$$

#### 三种准则对比

| 准则 | 公式 | 适合干什么 | 你最该记的点 |
|------|------|-----------|-------------|
| **Gini** | $1-\sum p_{mk}^2$ | **最常用分裂准则** | 越小越纯，计算快 |
| **信息熵** | $-\sum p_{mk}\log p_{mk}$ | 分裂准则 | 和 Gini 一个思路，只是写法不同 |
| **错误率** | $1-\max_k p_{mk}$ | **更适合评估，不适合分裂** | 对纯度变化不够敏感 |

> **考试速记：** 真正拿来切树的主要是 **Gini / 熵**；错误率一般不是分裂首选。

---

## 4.3 剪枝：代价复杂度剪枝 ⭐⭐

> **一句话：** 剪枝就是先长一棵很大的树，再往回砍，防止过拟合。

$$C_\alpha(T) = \text{RSS/Impurity}(T) + \alpha|T|$$

| $\alpha$值 | 效果 | 通俗理解 |
|-----------|------|---------|
| $\alpha=0$ | 无惩罚，保留完整大树 | 完全不怕复杂，最容易过拟合 |
| $\alpha$增大 | 树变小 | 越来越保守 |
| $\alpha\to\infty$ | 仅根节点 | 几乎不分裂，极端欠拟合 |

> **考试最爱问：** $\alpha$ 越大，树越小；最优 $\alpha$ 通常靠 **交叉验证** 选择。

---

## 4.4 装袋法（Bagging）

### 核心思想

> **一句话：** Bagging = 对训练集反复做 Bootstrap 抽样，各训一棵完整树，最后把结果平均/投票。

Bootstrap抽样（有放回，$n$个样本）→ 训练完整树 → 重复$B$次 → **平均/投票**

$$\hat f_{bag}(x) = \frac{1}{B}\sum_{b=1}^B T_b(x) \quad \text{（回归）}$$

### 关键性质

| 性质 | 内容 |
|------|------|
| 训练方式 | **并行**（各树独立）|
| 主要改善 | 降低**方差**（偏差基本不变）|
| OOB样本比例 | $\approx e^{-1}\approx 36.8\%$ 未被选中 |
| 可解释性 | 失去单棵树的可解释性 |

### 考试重点

- 看到 **Bootstrap + 平均/投票** 就想到 Bagging
- 看到 **并行**、**降方差** 也优先想到 Bagging
- OOB 比例常考结论：**约 36.8%**

### OOB误差

$$P(\text{OOB}) = \left(1-\frac{1}{n}\right)^n \approx e^{-1} \approx 0.368$$

对每个训练样本，用未选中它的那些树做预测，计算预测误差。

> **一句话记忆：** OOB 误差就是 Bagging / RF 自带的“免费验证集误差”。

---

## 4.5 随机森林（Random Forest）

### 与Bagging的唯一区别

> **一句话：** 随机森林 = Bagging + 每次分裂时随机抽一部分特征。

**每次节点分裂时，随机选取$m$个特征**（而非全部$p$个）

$$m \approx \sqrt{p} \text{（分类）}, \quad m \approx p/3 \text{（回归）}$$

### 为什么比Bagging更稳

特征随机化 → 树和树没那么像 → 树间相关性降低 → 平均之后方差更小。

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 何时随机选特征 | "RF什么时候随机选特征？" | **每次节点分裂时**重新随机选，不是一次性 |
| $m=p$时 | "$m=p$时RF是什么？" | 退化为标准Bagging |
| 为何比Bagging好 | "RF为何比Bagging方差更低？" | 特征随机化使树去相关，平均后更稳定 |
| OOB误差 | "RF如何估计泛化误差？" | 同Bagging，用OOB样本，无需额外交叉验证 |

### 你最该记的 3 句话

1. **RF 本质上还是 Bagging。**
2. **它的独特点在于：每次分裂随机抽特征。**
3. **主要作用：进一步降方差。**

---

## 4.6 提升法（Boosting）

### Gradient Boosting（梯度提升）

> **一句话：** Boosting 不是很多树一起投票，而是一棵接一棵地补前面模型的错误。

**核心思想：** 串行，每棵树拟合**当前预测的残差**（损失函数负梯度）

$$f_0(x) = \bar y \quad \text{（初始化）}$$

$$r_{ib} = y_i - f_{b-1}(x_i) \quad \text{（MSE损失下的残差）}$$

$$f_b(x) = f_{b-1}(x) + \lambda \cdot T_b(x) \quad \text{（更新）}$$

### 你真正要懂的意思

- 第1棵树先给一个粗糙预测
- 第2棵树去补第1棵树的错
- 第3棵树再补前两棵的错
- 所以它是**串行**的，不像 Bagging / RF 那样并行

### AdaBoost（自适应提升，分类）

> **一句话：** 分错的点，下一轮给更大权重，让后面的树更关注这些难点。

### Boosting关键超参数

| 参数 | 含义 | 你最该记的点 |
|------|------|-------------|
| **B（树数）** | 弱学习器总数 | 太大可能过拟合 |
| **$\lambda$（学习率）** | 每棵树贡献缩放 | 越小越稳，但通常要更多树 |
| **d（交互深度）** | 每棵树最大深度 | 常用浅树；$d=1$ 接近加法模型 |

> **考试口诀：** 小学习率 + 多棵树，通常更稳；Boosting 常用**浅树**，而不是完整大树。

---

## 4.7 五种方法综合对比表 ⭐⭐⭐

| 方法 | 是否串行/并行 | 树的数量 | 特征选取 | 树的深度 | 主要降低 | 优点 | 缺点 |
|------|------------|---------|---------|---------|---------|------|------|
| **回归树** | 单棵（N/A）| 1 | 全部$p$个 | 可深 | — | 可解释，快速 | 高方差，不稳定 |
| **分类树** | 单棵（N/A）| 1 | 全部$p$个 | 可深 | — | 可解释，非线性 | 高方差，易过拟合 |
| **Bagging** | **并行** | $B$（大）| 全部$p$个 | **深**（完整树）| **方差** | 稳定，OOB误差 | 失去可解释性 |
| **随机森林** | **并行** | $B$（大）| **随机$m$个** | **深**（完整树）| **方差**（更多）| 去相关，变量重要性 | 超参数多，黑箱 |
| **Boosting** | **串行** | $B$（中等）| 全部（浅树）| **浅**（$d=1\sim3$）| **偏差** | 精度高，灵活损失函数 | 慢，易过拟合，对异常值敏感 |

---

## 4.8 Bagging vs Random Forest vs Boosting 核心区别 ⭐⭐⭐

| 比较维度 | Bagging | Random Forest | Boosting |
|---------|---------|--------------|---------|
| **训练方式** | **并行** | **并行** | **串行** |
| **树的相关性** | 高 | 低（特征随机化）| 依赖（每棵树依赖前一棵）|
| **主要改善** | 降低**方差** | 降低**方差**（更多）| 降低**偏差** |
| **每棵树深度** | 深（完整树）| 深（完整树）| **浅**（$d=1\sim3$）|
| **过拟合风险** | 低 | 低 | 高（B太大时）|
| **对异常值** | 鲁棒 | 鲁棒 | **敏感**（AdaBoost尤甚）|
| **超参数** | 少（主要B）| 中（B、m）| **多**（B、λ、d）|
| **能否并行** | ✅ | ✅ | ❌ |
| **精度排名** | 中 | 中高 | **最高** |

**核心口诀：**
> - **Bagging**：Bootstrap + Average，**并行**，**降方差**
> - **RF**：Bagging + 随机特征，**去相关**，**方差更低**
> - **Boosting**：**串行**，专注残差，**降偏差**，精度最高但慢

---

## 4.9 变量重要性综合对比

| 方法 | 计算方式 | 特点 |
|------|---------|------|
| 单棵树 | 该特征所有节点RSS/Gini减少量之和 | 依赖树结构 |
| Bagging/RF（MDI）| 所有树中不纯度减少量均值 | 对高基数特征有偏 |
| RF（Permutation）| 打乱特征后OOB误差的增加量 | **无偏，更可靠** |
| Boosting | 所有树中该特征的分裂增益之和 | 受学习率影响 |

---

## 4.10 树方法与集成学习高频陷阱

| 陷阱 | 正确理解 |
|------|---------|
| Boosting能并行 | **Boosting串行**，每棵树依赖上一棵 |
| RF训练前随机选特征 | **每次节点分裂时**重新随机选$m$个特征 |
| Gini越大越好 | Gini越**小**越纯，目标是**最小化**加权Gini |
| B越大Boosting越好 | B太大会**过拟合**，需CV选B |
| Bagging/Boosting都用浅树 | **Bagging/RF用深树**；**Boosting用浅树** |
| RF主要降低偏差 | RF主要降低**方差**，偏差与单棵树相近 |
| 分类树用RSS分裂 | **分类树用Gini/熵**，回归树用RSS |
| OOB误差=50%未选中 | 约**36.8%**未被选中：$(1-1/n)^n\approx e^{-1}$ |
| AdaBoost所有树权重相同 | 权重为$\alpha_b$，误差率越低权重越高 |
| $m=p$时RF比Bagging好 | $m=p$时RF**退化为**标准Bagging |

---

# 五、逻辑斯蒂回归专项

## 5.1 模型公式

**简单逻辑回归（1个预测变量）：**

$$\log\left(\frac{p}{1-p}\right) = \beta_0 + \beta_1X$$

$$p = \frac{e^{\beta_0+\beta_1X}}{1+e^{\beta_0+\beta_1X}} = \frac{1}{1+e^{-(\beta_0+\beta_1X)}}$$

**多元逻辑回归（$p$个预测变量）：**

$$\log\left(\frac{p}{1-p}\right) = \beta_0 + \beta_1X_1 + \cdots + \beta_pX_p$$

> **核心：左边是 log-odds（对数几率），不是概率本身**

---

## 5.2 如何看回归输出表格 ⭐⭐

```
          系数      标准差    Z统计量    P值
Intercept  -3.5041   0.0707   -49.55   <0.0001
student     0.4049   0.1150     3.52    0.0004
```

| 列 | 含义 | 考试关注点 |
|---|------|-----------|
| **系数** | $\hat\beta$：X每增加1单位，log-odds的变化量 | 方向（正/负）和大小 |
| **标准差** | $SE(\hat\beta)$ | 估计精度 |
| **Z统计量** | $Z=\hat\beta/SE$ | $|Z|>1.96$则p<0.05 |
| **P值** | 双尾检验 | **<0.05则显著** |

**速读口诀：** 看p值判显著，看系数正负判方向

---

## 5.3 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| **判断变量是否显著**（最高频）| "是否对预测有影响？" | "p值=0.0004<0.05，拒绝$H_0$，**有显著影响**" |
| **系数解释** | "student系数0.4049含义？" | "student身份使log-odds增加0.4049，即违约**几率**增大$e^{0.4049}\approx1.5$倍" |
| **计算违约概率** | "是学生时的违约概率？" | 代入公式：$p=e^{-3.5041+0.4049\times1}/(1+e^{-3.5041+0.4049\times1})$ |
| **系数一正一负**（辛普森悖论）| "简单模型正，多元模型负，为什么？" | **辛普森悖论/混淆效应**，balance是混淆变量，控制它后方向改变 |

---

## 5.4 概率计算步骤（必考）⭐⭐⭐

```
Step 1：写出 log-odds 表达式
        log(p/1-p) = β₀ + β₁X₁ + ...

Step 2：代入已知数值，计算 log-odds

Step 3：套概率公式
        p = e^(log-odds) / (1 + e^(log-odds))
```

---

## 5.5 Odds Ratio（几率比）⭐⭐

$$OR = e^{\beta_j}$$

| OR | 含义 |
|---|------|
| $>1$（$\beta>0$）| X增加1单位，违约**几率**增大OR倍 |
| $<1$（$\beta<0$）| X增加1单位，违约几率下降 |
| $=1$（$\beta=0$）| X无影响 |

**例：** student系数=0.4049 → $OR=e^{0.4049}\approx1.50$ → 学生违约几率是非学生的**1.5倍**

---

## 5.6 辛普森悖论（Simpson's Paradox）⭐⭐⭐

**现象：** 表1（简单模型）student系数为**正**，表2（多元模型）student系数为**负**

**原因：**
```
表1（忽略balance）：
  学生 → 往往有更高的信用卡余额（balance）
  高余额 → 违约率高
  ∴ 整体看，学生违约率更高 → 系数为正

表2（控制balance）：
  在相同balance水平下比较
  ∴ 学生反而违约率更低 → 系数为负
```

**balance是混淆变量（confounder）**，同时与"是否学生"和"违约率"相关。

**答题模板：**
> "两表中student系数一正一负，这是由于**混淆变量（confounding variable）**的存在，具体表现为**辛普森悖论**。在简单模型中，student的正效应是由更高的balance引起的；控制balance后（多元模型），学生的真实效应为负。"

---

## 5.7 简单vs多元逻辑回归对比

| 对比 | 简单逻辑回归 | 多元逻辑回归 |
|------|------------|------------|
| 预测变量 | 1个 | 多个 |
| 系数解释 | X增加1单位，log-odds变化$\beta$ | **控制其他变量后**，$X_j$增加1单位，log-odds变化$\beta_j$ |
| 混淆问题 | 可能存在混淆 | 通过控制其他变量减少混淆 |
| 辛普森悖论 | 可能出现 | 可以揭示真实效应 |

---

## 5.8 Default / Credit 数据集答题模板 ⭐⭐⭐

**最常见问法：**

| 问法 | 标准答法 |
|------|---------|
| "变量是否显著？" | "看p值，若 $p<0.05$，则拒绝 $H_0:\beta_j=0$，认为该变量对违约概率有显著影响。" |
| "解释student系数" | "student=1 相比 student=0，使 log-odds 改变 $\hat\beta$，对应几率比为 $e^{\hat\beta}$。" |
| "计算违约概率" | 先算 $\eta=\hat\beta_0+\hat\beta_1x_1+\cdots$，再代入 $p=\frac{e^\eta}{1+e^\eta}$ |
| "为什么简单模型和多元模型系数变号？" | "这是混淆变量引起的**辛普森悖论**；控制关键变量后，真实效应方向可能改变。" |

**哑变量化简模板：**

若

$$\log\frac{p}{1-p}=\beta_0+\beta_1\cdot income+\beta_2\cdot student$$

则：

- 当 `student=0`：$\log\frac{p}{1-p}=\beta_0+\beta_1\cdot income$
- 当 `student=1`：$\log\frac{p}{1-p}=(\beta_0+\beta_2)+\beta_1\cdot income$

若还有交互项 $student\times income$，则学生组的斜率变为 $\beta_1+\beta_3$。

---

# 六、距离方法与支持向量机模块

## 6.1 K近邻（KNN）⭐⭐

**分类规则：** 对测试点 $x_0$，找最近的 $K$ 个训练样本，多数投票：

$$\hat Y(x_0)=\mathrm{mode}\{Y_i:i\in \mathcal N_K(x_0)\}$$

**回归规则：**

$$\hat f(x_0)=\frac{1}{K}\sum_{i\in\mathcal N_K(x_0)}Y_i$$

| $K$ 的大小 | 决策边界 | 偏差 | 方差 |
|-----------|---------|------|------|
| **小** | 很曲折，很灵活 | 低 | 高 |
| **大** | 更平滑，更稳定 | 高 | 低 |

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| K 的作用 | "$K$ 变大后模型如何变化？" | 边界更平滑，偏差上升，方差下降 |
| K=1 | "1-NN 的训练误差是多少？" | 对训练集几乎为0，但更容易过拟合 |
| 距离度量 | "KNN 为什么常需标准化？" | 各变量量纲不同会扭曲距离，故通常先标准化 |
| 方法类型 | "KNN 是参数模型吗？" | **非参数方法**，不显式估计系数 |

## 6.2 支持向量分类器与SVM ⭐⭐

### 一句话理解 SVM（先别怕公式）

> **SVM 做的事：** 在两类样本中间找一条分类边界，而且要让这条边界离两边最近的点都尽量远，也就是让**间隔（margin）最大**。

**支持向量：** 离分类边界最近、真正决定超平面的样本点；或者违反间隔约束的样本点。

> **考试里最该记的一句话：** 不是所有样本都同样重要，**最靠近边界的那些点最重要**，它们就是支持向量。

### 软间隔分类器

$$\min_{\beta_0,\beta,\xi}\ \frac{1}{2}\|\beta\|^2 + C\sum_{i=1}^n\xi_i$$

$$\text{s.t.}\quad y_i(\beta_0+\beta^Tx_i)\ge 1-\xi_i,\qquad \xi_i\ge0$$

**怎么读这个式子：**
- $\|\beta\|^2$：希望间隔尽量大
- $\xi_i$：允许样本跑进间隔甚至被分错（松弛变量）
- $C$：控制“对犯错这件事有多较真”

> **软间隔（soft margin）** = 允许少量分错，换一个更稳的分类边界。

### 常见核函数

| 核函数 | 公式 | 说明 |
|------|------|------|
| 线性核 | $K(x,x')=x^Tx'$ | 线性边界 |
| 多项式核 | $K(x,x')=(1+x^Tx')^d$ | 可产生非线性边界 |
| RBF核 | $K(x,x')=\exp(-\gamma\|x-x'\|^2)$ | 最常考，边界灵活 |

> **核函数是干什么的：** 如果原始空间里分不开，就把数据映射到更高维空间，再在那里做线性分割。

### 参数影响（非常爱考）

| 参数 | 变大后的效果 | 记忆方式 |
|------|-------------|---------|
| **$C$** | 对误分类惩罚更重，边界更贴训练集，偏差下降、方差上升 | 大 $C$ = 更"较真" |
| **$\gamma$**（RBF） | 单个样本影响范围更小，边界更曲折 | 大 $\gamma$ = 更局部 |

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| SVM 的目标 | "SVM 想做什么？" | 在分类边界两侧找**最大间隔**的超平面 |
| 支持向量 | "哪些点决定超平面？" | 只有支持向量真正决定边界 |
| 软间隔 | "为什么要允许误分类？" | 现实数据常常不可完全线性分开，允许少量误分类可提升泛化能力 |
| $C$ 的含义 | "软间隔里 $C$ 控制什么？" | 控制对误分类/违反间隔样本的惩罚强度 |
| $\gamma$ 的含义 | "RBF核中 $\gamma$ 变大后会怎样？" | 每个样本影响更局部，边界更曲折，更容易过拟合 |
| SVM vs 逻辑回归 | "二者区别？" | 逻辑回归直接建模概率；SVM关注分类边界与间隔 |
| 非线性边界 | "SVM 如何处理非线性？" | 通过核函数在高维特征空间实现线性分割 |

### 考试最爱怎么问

| 题型 | 常见问法 | 你该怎么做 |
|------|---------|-----------|
| **概念题** | "支持向量是什么？" | 先答“离边界最近、决定超平面的点” |
| **参数题** | "$C$ 增大时会怎样？" | 固定答：更贴训练集，偏差降、方差升 |
| **核函数识别题** | "这是什么核函数？" | 看见 $\exp(-\gamma\|x-x'\|^2)$ 就答 RBF 核 |
| **对比题** | "SVM 和逻辑回归有何区别？" | 一个找最大间隔，一个建模概率 |
| **公式识别题** | "这个目标函数是什么模型？" | 看见 $\frac12\|\beta\|^2 + C\sum\xi_i$ 就想到软间隔 SVM |

### 五句背诵版（时间不够就背这个）

1. **SVM = 找最大间隔分类边界。**
2. **支持向量 = 离边界最近、决定边界的关键样本。**
3. **软间隔 = 允许少量误分类，换取更稳的边界。**
4. **$C$ 大 = 更不容错 = 边界更复杂 = 更易过拟合。**
5. **RBF核里 $\gamma$ 大 = 影响更局部 = 边界更曲折。**

### 高频陷阱

| 陷阱 | 正确理解 |
|------|---------|
| $C$ 越大正则化越强 | 错。$C$ 越大越不愿容忍误分类，模型更复杂 |
| SVM 一定给出概率 | 标准 SVM 输出分类边界，不天然输出概率 |
| 非线性边界一定靠加高次项 | 核函数本身就能隐式实现非线性映射 |
| 支持向量就是所有样本 | 错。真正决定边界的是靠近边界的那部分样本 |
| $\gamma$ 越大越平滑 | 错。$\gamma$ 越大，边界通常越曲折 |

### SVM 答题模板 ⭐⭐⭐

**若题目问："SVM 中把 $C$ 调大，会怎样？"**

> $C$ 越大，对误分类样本的惩罚越重，模型越不愿意容忍训练误差，因此决策边界更贴近训练集，模型复杂度上升，偏差下降、方差上升，更容易过拟合。

**若题目问："什么是支持向量？"**

> 支持向量是离分类边界最近、真正决定分类超平面的样本点；去掉非支持向量通常不会改变决策边界。

**若题目问："SVM 如何处理非线性边界？"**

> SVM 可通过核函数把数据隐式映射到更高维特征空间，在高维空间中实现线性分割，从而得到原空间中的非线性边界。

**若题目问："RBF核里 $\gamma$ 变大后会怎样？"**

> $\gamma$ 越大，每个样本只在更局部的区域内起作用，因此决策边界会更曲折、更复杂，也更容易过拟合。

### 和例题串讲联动

- 对应例题：`10.14 SVM 题：核函数、$C$ 和 $\gamma$ 一起考`
- 复习顺序建议：先看本节的“一句话理解”与“五句背诵版”，再去做 `10.14`
- 如果只剩考前最后一轮：优先背 **支持向量 / $C$ / RBF核 / $\gamma$** 这四个点

---

# 七、非线性回归与样条模块

## 7.1 基函数展开（Basis Expansion）⭐⭐

把原始变量 $X$ 变成若干新特征 $b_1(X),b_2(X),\ldots,b_M(X)$ 后，再做线性回归：

$$Y=\beta_0+\beta_1b_1(X)+\cdots+\beta_Mb_M(X)+\varepsilon$$

> **核心理解：** 模型对参数仍是线性的，但对原始变量 $X$ 可以是非线性的。

## 7.2 三次样条与自然样条 ⭐⭐

**带结点 $\xi_1,\ldots,\xi_K$ 的三次样条常写成：**

$$f(x)=\beta_0+\beta_1x+\beta_2x^2+\beta_3x^3+\sum_{k=1}^K\beta_{k+3}(x-\xi_k)_+^3$$

其中

$$ (x-\xi_k)_+^3 = \begin{cases}(x-\xi_k)^3,&x>\xi_k\\0,&x\le \xi_k\end{cases} $$

**自然样条：** 在边界两端强制为线性，因此比普通三次样条更稳定。

### 核心考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 结点作用 | "结点右侧多出的项是什么？" | 是截断幂基 $(x-\xi_k)_+^3$ |
| 三次样条连续性 | "为什么样条看起来平滑？" | 因为函数值、一阶导、二阶导在结点处连续 |
| 自然样条优势 | "为什么边界更稳？" | 两端被限制为线性，减少边界振荡 |

## 7.3 光滑样条（Smoothing Spline）⭐⭐⭐

$$\min_f\sum_{i=1}^n(y_i-f(x_i))^2+\lambda\int [f''(t)]^2dt$$

| $\lambda$ | 模型形状 | 偏差/方差 |
|-----------|---------|-----------|
| $\lambda=0$ | 尽量穿过所有点，最不平滑 | 偏差低，方差高 |
| $\lambda$ 增大 | 更平滑 | 偏差上升，方差下降 |
| $\lambda\to\infty$ | 趋近直线 | 偏差高，方差低 |

> **考试模板：** "$\lambda$ 控制拟合与光滑的平衡；$\lambda$ 越大，曲线越平滑，偏差越大，方差越小。"

## 7.4 广义加性模型（GAM）⭐⭐

$$Y=\beta_0+f_1(X_1)+f_2(X_2)+\cdots+f_p(X_p)+\varepsilon$$

若是二分类问题，则常写为：

$$\log\frac{p}{1-p}=\beta_0+f_1(X_1)+\cdots+f_p(X_p)$$

### GAM 的意义

| 优点 | 含义 |
|------|------|
| 可解释 | 可以单独画出每个 $f_j(X_j)$ 的边际效应 |
| 能捕捉非线性 | 比纯线性模型更灵活 |
| 比全非参数稳 | 仍保留加性结构，避免维数灾难过重 |

## 7.5 非线性回归高频陷阱

| 陷阱 | 正确认识 |
|------|---------|
| 加了基函数就不是线性模型 | 对参数仍是线性的，只是对原始变量非线性 |
| 结点越多一定越好 | 结点太多会过拟合，需CV或经验选择 |
| 光滑样条 $\lambda$ 越大越灵活 | 错，$\lambda$ 越大越平滑 |
| GAM 完全不可解释 | 恰恰相反，GAM 比黑箱模型更容易看单变量效应 |

---

# 八、降维与聚类模块

## 8.1 主成分分析（PCA）⭐⭐

> **一句话：** PCA 就是把很多原变量压缩成少数几个“综合变量”，而且这些综合变量尽量保留原数据里最多的信息（方差）。

**第一主成分：** 在线性组合中寻找方差最大的方向

$$Z_1=\phi_{11}X_1+\phi_{21}X_2+\cdots+\phi_{p1}X_p$$

满足 $\sum_{j=1}^p\phi_{j1}^2=1$。

### PCA 记忆要点

| 考点 | 标准答法 |
|------|---------|
| 第一主成分 | 方差最大的线性组合 |
| 后续主成分 | 在与前面主成分正交的前提下，方差尽可能大 |
| 预处理 | 通常要先中心化；量纲差异大时还要标准化 |
| 载荷符号 | $\phi$ 与 $-\phi$ 表示同一主成分方向，符号不重要 |

> **考试最爱问：** 第一主成分是什么、方差贡献率怎么算、载荷整体变号会不会变含义。

## 8.2 K均值聚类（K-means）⭐⭐⭐

> **一句话：** K均值就是把样本分成 K 组，让组内尽量像、组间尽量不像。

$$\min_{C_1,\ldots,C_K}\sum_{k=1}^K\sum_{i\in C_k}\|x_i-\bar\mu_k\|^2$$

其中 $\bar\mu_k$ 是第 $k$ 类的样本均值（质心）。

**算法步骤：**
1. 初始化 $K$ 个簇或 $K$ 个中心
2. 把每个点分到最近中心
3. 重新计算各簇质心
4. 重复直到不再变化

### K均值常考点

| 考点 | 考试问法 | 标准答法 |
|------|---------|---------|
| 目标函数 | "K均值最小化什么？" | 组内平方和（within-cluster sum of squares） |
| 初始值影响 | "不同初始类会影响结果吗？" | **会**，因为K均值可能停在局部最优 |
| 计算题套路 | "写出一次迭代后的聚类结果" | 先算质心，再算距离，再重分组 |
| K 的选择 | "怎么选聚类数？" | 常用肘部法、轮廓系数、业务先验 |

> **考试速记：** K均值最怕两件事：**初始值乱选**、**变量不标准化**。

## 8.3 层次聚类（Hierarchical Clustering）⭐⭐

> **一句话：** 层次聚类不是先定 K，而是先把样本一点点并起来，最后再从树状图上决定分几类。

**凝聚式层次聚类：** 从每个样本自成一类开始，不断合并最近的两类。

### 常见 linkage 方法

| 方法 | 类间距离 | 特征 |
|------|---------|------|
| Single | 两类最近点距离 | 易链化 |
| Complete | 两类最远点距离 | 类更紧凑 |
| Average | 两类所有点距离平均 | 折中 |
| Centroid | 两类质心距离 | 可能出现倒置 |
| Ward | 合并后类内平方和增量 | 常考，较稳定 |

> **倒置现象提醒：** 层次聚类里最常与倒置现象联系的是 **Centroid**；若老师课堂口径对其他 linkage 也有特别强调，考试以课堂定义为准。

> **考试速记：**
> - `Single`：最容易链化
> - `Centroid`：最常和倒置现象联系
> - 树状图要看“哪一段高度跳得最大”

## 8.4 PCA 与聚类高频陷阱

| 陷阱 | 正确认识 |
|------|---------|
| PCA 是监督学习 | 错。PCA 是**无监督降维** |
| 第一主成分就是相关性最大方向 | 更准确地说，是**方差最大方向** |
| K均值一定找到全局最优 | 错，只保证局部最优 |
| 聚类前不用标准化 | 若量纲差异明显，聚类结果会被大尺度变量主导 |

---

# 九、重抽样与代码题模块

## 9.1 Bootstrap ⭐⭐

> **一句话：** Bootstrap 就是把手头这份样本反复“有放回重抽”，用来模拟统计量的波动。

**思想：** 从原始样本中**有放回抽样**，重复很多次，模拟统计量的抽样分布。

若每次抽样仍抽 $n$ 个样本，则一个固定样本点在某次 Bootstrap 中**没被抽到**的概率约为：

$$\left(1-\frac{1}{n}\right)^n\approx e^{-1}\approx 0.368$$

### 核心考点

| 考点 | 标准答法 |
|------|---------|
| 为什么能估计标准误 | 因为 Bootstrap 近似了统计量的抽样分布 |
| 95%区间怎么做 | 取 Bootstrap 估计值的 2.5% 与 97.5% 分位数 |
| 与CV区别 | Bootstrap偏重**不确定性/标准误**；CV偏重**泛化误差** |

> **考试最爱记忆点：** `0.368`、percentile 区间、和 CV 的区别。

## 9.2 高频 R 代码识别表 ⭐⭐

> **一句话：** 代码题先别慌，先认函数名，它基本已经告诉你这是什么模型了。

| 函数/关键词 | 对应内容 | 考试最爱问什么 |
|------------|---------|---------------|
| `lm()` | 线性回归 | 系数、$R^2$、显著性 |
| `glm(..., family=binomial)` | 逻辑回归 | 概率计算、OR、变量显著性 |
| `glmnet(alpha=0)` | Ridge | $\lambda$ 作用、是否能把系数压成0 |
| `glmnet(alpha=1)` | LASSO | 稀疏性、变量选择 |
| `cv.glmnet()` | 交叉验证选 $\lambda$ | `lambda.min` 与 `lambda.1se` |
| `gbm()` | Boosting | 树数、学习率、深度 |
| `kmeans()` | K均值聚类 | centers、迭代、初始值 |
| `hclust()` | 层次聚类 | linkage 方法、树状图 |
| `prcomp()` | PCA | 载荷、方差解释比例 |
| `boot()` | Bootstrap | 重抽样、标准误、区间 |

## 9.3 代码题答题顺序模板 ⭐⭐⭐

**看到代码题，按下面五句写：**

1. 这段代码在做哪类模型/算法。
2. 训练集和测试集（或CV）是怎么分的。
3. 关键参数是什么，参数变大会怎样。
4. 用什么指标比较模型（MSE、误分类率、CV误差、OOB误差）。
5. 最后一句点名：哪个模型更好，依据是什么。

> **代码题拿分秘诀：** 就算算不出最后数值，也要把 `模型名称 + 参数作用 + 评价指标` 三件事写出来，通常就能拿到大半分。

---

# 十、例题串讲（完整版）

> **怎么用这一部分：** 不要只看答案。先自己做，再对照“解题步骤”和“标准答法”。你会发现考试真正考的不是死算，而是**先识别题型，再套固定话术**。
> **这一版已经补成完整版：** 每个主模块至少配了 1 道最像考试的例题，优先覆盖你们近年最常见的表格题、参数题、计算题、对比题和代码题。

## 10.1 线性回归表格题：先判显著，再解释系数 ⭐⭐⭐

**题目：** 已知回归输出如下：

```
              Estimate  Std.Error  t value  Pr(>|t|)
(Intercept)    2.500     0.700      3.57     0.001
TV             0.045     0.005      9.00    <0.001
radio          0.180     0.070      2.57     0.015
newspaper      0.001     0.006      0.17     0.870
```

回答：
1. 哪些变量对 `sales` 有显著影响？
2. 如何解释 `radio` 的系数？
3. 当 `TV=100`、`radio=10`、`newspaper=20` 时，预测销售额是多少？

**解题步骤：**

- 第1步：看 p 值
  - `TV` 的 p 值 `<0.001`，显著
  - `radio` 的 p 值 `0.015<0.05`，显著
  - `newspaper` 的 p 值 `0.870>0.05`，不显著
- 第2步：解释多元回归系数时，必须补一句 **“其他变量保持不变”**
- 第3步：代入预测公式

$$\hat y = 2.5 + 0.045\times 100 + 0.18\times 10 + 0.001\times 20 = 8.82$$

**标准答法：**

> `TV` 和 `radio` 对销售额有显著影响，因为它们的 p 值都小于 0.05；`newspaper` 不显著。`radio` 系数 0.18 的含义是：**在 TV 和 newspaper 保持不变的条件下**，radio 每增加 1 个单位，sales 平均增加 0.18 个单位。当 `TV=100`、`radio=10`、`newspaper=20` 时，预测值为 8.82。

**这题真正考什么：**
- 会不会先看 p 值
- 会不会解释“偏效应”
- 会不会把表格题翻译成人话

---

## 10.2 逻辑回归概率题：先算 log-odds，再转概率 ⭐⭐⭐

**题目：** 已知模型

$$\log\frac{p}{1-p} = -4 + 0.5\cdot student + 0.004\cdot balance$$

其中 `student=1` 表示学生，`student=0` 表示非学生。

回答：
1. 当某人是学生且 `balance=600` 时，违约概率是多少？
2. `student` 的几率比（OR）是多少？如何解释？

**解题步骤：**

- 第1步：代入得到线性预测子

$$\eta = -4 + 0.5\times 1 + 0.004\times 600 = -1.1$$

- 第2步：由 log-odds 转成概率

$$p = \frac{e^{-1.1}}{1+e^{-1.1}} \approx 0.2497$$

- 第3步：算 OR

$$OR = e^{0.5} \approx 1.65$$

**标准答法：**

> 当 `student=1` 且 `balance=600` 时，先得到 log-odds 为 `-1.1`，所以违约概率约为 `0.25`。`student` 的 OR 为 `e^{0.5}\approx1.65`，表示在其他变量不变时，学生的违约**几率**约为非学生的 1.65 倍。

**这题真正考什么：**
- 概率题不能直接把右边当概率
- 逻辑回归系数解释的是 **log-odds** 或 **odds ratio**，不是概率本身

---

## 10.3 混淆矩阵推广题：会不会把 TPR/FPR 乘回去 ⭐⭐⭐

**题目：** 某模型在测试集上的混淆矩阵如下：

|  | 预测负 | 预测正 |
|--|-------|-------|
| 实际负 | 170 | 30 |
| 实际正 | 20 | 80 |

回答：
1. 总体错误率是多少？
2. TPR 和 FPR 各是多少？
3. 若新测试集中有 1000 个真实正例、2000 个真实负例，则该模型大约能正确识别多少个正例？又会错判多少个负例为正例？

**解题步骤：**

- 第1步：先认四格（按课堂常用表头：第一行是真实负，第二行是真实正）
  - `TN=170, FP=30, FN=20, TP=80`
- 第2步：算错误率

$$\text{Error Rate} = \frac{FP+FN}{TP+FP+FN+TN} = \frac{30+20}{300} = \frac{1}{6}\approx 0.167$$

- 第3步：算 TPR 与 FPR

$$TPR = \frac{80}{80+20}=0.8, \qquad FPR = \frac{30}{30+170}=0.15$$

- 第4步：推广到新样本规模

$$\text{正确识别的正例数}=TPR\times 1000 = 800$$

$$\text{错判为正例的负例数}=FPR\times 2000 = 300$$

**标准答法：**

> 总体错误率约为 0.167。模型的 TPR 为 0.8，FPR 为 0.15。因此在新的测试集中，若有 1000 个真实正例，则大约能正确识别 800 个；若有 2000 个真实负例，则大约会把 300 个负例误判为正例。

**这题真正考什么：**
- 会不会把 TP/FN/FP/TN 认清
- 会不会把比例推广回人数
- 一旦出现“新样本规模”，第一反应就是 **TPR 乘正例数，FPR 乘负例数**

---

## 10.4 参数变化题串讲：K、C、$\lambda$ 怎么答 ⭐⭐⭐

### 小题 A：KNN 里的 $K$

**题目：** 若 KNN 中的 $K$ 从 1 增大到 50，模型会发生什么变化？

**标准答法：**

> 随着 $K$ 增大，决策边界会变得更平滑，模型灵活性下降，偏差上升、方差下降。$K=1$ 时训练误差通常很低，但更容易过拟合。

### 小题 B：SVM 里的 $C$

**题目：** SVM 中若把 $C$ 调得很大，会怎样？

**标准答法：**

> $C$ 越大，对误分类样本的惩罚越重，模型越不愿意容忍训练误差，因此决策边界往往更贴近训练集，偏差下降、方差上升，更容易过拟合。

### 小题 C：光滑样条里的 $\lambda$

**题目：** 光滑样条中，$\lambda=0$ 和 $\lambda\to\infty$ 分别意味着什么？

**标准答法：**

> 当 $\lambda=0$ 时，不惩罚曲线弯曲程度，模型会尽量贴住训练点，容易过拟合；当 $\lambda\to\infty$ 时，平滑惩罚极强，曲线趋近于一条直线，偏差高、方差低。

**这一类题的通用口诀：**

> 看到参数题，先判断：**参数变大后模型是更灵活，还是更平滑/更保守？** 然后立刻接一句：**偏差怎么变，方差怎么变。**

---

## 10.5 K均值迭代题：先算质心，再重新分组 ⭐⭐⭐

**题目：** 一维样本点为 `1, 3, 4, 8, 10`，做 `K=2` 的 K-means。
初始两类中心分别取 `1` 和 `4`。请写出迭代后的最终聚类结果。

**解题步骤：**

### 第一次分组

- 到中心 `1` 的距离：
  - `1 -> 0`
  - `3 -> 2`
  - `4 -> 3`
  - `8 -> 7`
  - `10 -> 9`
- 到中心 `4` 的距离：
  - `1 -> 3`
  - `3 -> 1`
  - `4 -> 0`
  - `8 -> 4`
  - `10 -> 6`

所以第一次分组为：

- $C_1=\{1\}$
- $C_2=\{3,4,8,10\}$

### 更新质心

$$\mu_1=1, \qquad \mu_2=\frac{3+4+8+10}{4}=6.25$$

### 第二次分组

分别比较每个点到 `1` 与 `6.25` 的距离：

- `1` 更靠近 `1`
- `3` 距 `1` 为 2，距 `6.25` 为 3.25，所以归到第一类
- `4` 距 `1` 为 3，距 `6.25` 为 2.25，所以归到第二类
- `8`、`10` 显然归到第二类

所以第二次分组为：

- $C_1=\{1,3\}$
- $C_2=\{4,8,10\}$

### 再更新质心

$$\mu_1=\frac{1+3}{2}=2, \qquad \mu_2=\frac{4+8+10}{3}=\frac{22}{3}\approx 7.33$$

此时再检查一次分组，不再变化，因此最终结果为：

$$C_1=\{1,3\}, \qquad C_2=\{4,8,10\}$$

**这题真正考什么：**
- K-means 题不要怕，本质就是反复做两件事：**算质心、重新分组**
- 初始值不同，最后结果可能不同，这也是考试爱问的点

---

## 10.6 代码题读法示例：先认模型，再看参数 ⭐⭐

**题目：** 阅读下面代码，并回答它在做什么。

```r
cvfit <- cv.glmnet(x, y, alpha = 1)
coef(cvfit, s = "lambda.1se")
```

**解题步骤：**

- `glmnet`：说明这是正则化回归
- `alpha = 1`：说明这是 **LASSO**，不是 Ridge
- `cv.glmnet`：说明是在用**交叉验证选择 $\lambda$**
- `lambda.1se`：说明不是选误差最小的最优点，而是选 **1-SE rule 下更简单的模型**

**标准答法：**

> 这段代码在做 **LASSO 回归**。其中 `cv.glmnet` 用交叉验证选择惩罚参数 $\lambda$，`alpha=1` 表示使用 L1 惩罚。`coef(cvfit, s = "lambda.1se")` 表示提取在 **1-SE rule** 下对应的系数，这通常会得到一个更简单、更稀疏的模型。

**代码题通用模板：**

1. 先说模型名称
2. 再说关键参数对应什么方法
3. 再说是在训练、调参还是评估
4. 最后说输出结果代表什么

---

## 10.7 交互项解释题：主效应不是固定效应 ⭐⭐⭐

**题目：** 已知模型

$$\hat y = 5 + 0.02\cdot TV + 0.10\cdot radio + 0.01\cdot TV\times radio$$

回答：
1. `TV × radio` 这一项的含义是什么？
2. 当 `radio=4` 时，`TV` 每增加 1 单位，对 `sales` 的边际影响是多少？
3. 若交互项显著，但 `TV` 主效应 p 值不显著，能否删去 `TV` 主效应？

**解题步骤：**

- 第1步：看到交互项，先写边际效应

$$\frac{\partial E(Y)}{\partial TV} = 0.02 + 0.01\cdot radio$$

- 第2步：代入 `radio=4`

$$\frac{\partial E(Y)}{\partial TV} = 0.02 + 0.01\times 4 = 0.06$$

- 第3步：回忆层次原则
  - 有交互项时，通常**不能只删主效应保留交互项**

**标准答法：**

> 交互项系数 0.01 表示：`radio` 每增加 1 单位，`TV` 对 `sales` 的边际效应增加 0.01。当 `radio=4` 时，`TV` 每增加 1 单位，`sales` 平均增加 0.06。若交互项显著，则即使主效应单独不显著，也通常不能删除主效应，因为要满足**层次原则**。

**这题真正考什么：**
- 会不会把交互项翻译成“一个变量改变另一个变量的效应”
- 会不会写出边际效应函数
- 会不会记住层次原则

---

## 10.8 CI vs PI 区分题：同一个中心，PI 永远更宽 ⭐⭐⭐

**题目：** 已知某点的预测值为 $\hat y^*=20$，并且：

$$MSE = 4, \qquad t_{0.025}\approx 2$$

对该点：

$$\text{均值响应的方差项}=0.30, \qquad \text{新个体预测的方差项}=1.30$$

回答：
1. 该点的 95% 置信区间（CI）是多少？
2. 该点的 95% 预测区间（PI）是多少？
3. 为什么 PI 更宽？

**解题步骤：**

- CI：

$$20 \pm 2\sqrt{4\times 0.30} = 20 \pm 2\sqrt{1.2} \approx 20 \pm 2.19$$

所以 CI 约为：

$$[17.81, 22.19]$$

- PI：

$$20 \pm 2\sqrt{4\times 1.30} = 20 \pm 2\sqrt{5.2} \approx 20 \pm 4.56$$

所以 PI 约为：

$$[15.44, 24.56]$$

**标准答法：**

> 95% 置信区间约为 `[17.81, 22.19]`，95% 预测区间约为 `[15.44, 24.56]`。预测区间更宽，是因为它除了考虑均值估计误差，还额外考虑了**新个体自身的随机波动**。

**这题真正考什么：**
- CI 是估计均值，PI 是预测单个新观测
- 同样中心下，**PI 一定比 CI 宽**

---

## 10.9 分类方法选择题：LDA、QDA、NB、逻辑回归怎么选 ⭐⭐⭐

**题目：** 现在有四个场景：

1. 样本量较小，各类协方差矩阵差不多相同；
2. 样本量较大，各类边界明显弯曲，协方差差异较大；
3. 特征很多、近似条件独立，比如文本词频；
4. 不想做分布假设，只想建模类别概率。

分别更适合选哪种方法？

**解题步骤：**

- 场景1 → **LDA**：小样本 + 等协方差
- 场景2 → **QDA**：边界更灵活，各类协方差不同
- 场景3 → **Naive Bayes**：高维 + 条件独立近似成立
- 场景4 → **逻辑回归**：判别式方法，不依赖正态分布假设

**标准答法：**

> 场景1 选 LDA，场景2 选 QDA，场景3 选 Naive Bayes，场景4 选逻辑回归。因为 LDA 假设共享协方差、方差较低，适合小样本；QDA 更灵活，适合边界弯曲且样本较大；Naive Bayes 在高维近独立特征下往往很有效；逻辑回归则不需要正态分布假设，并且直接建模后验概率。

**这题真正考什么：**
- 不是背公式，而是会把**场景关键词**翻译成方法选择
- 选择题里看到“小样本/高维/协方差相同/无分布假设”要条件反射

---

## 10.10 Ridge vs LASSO 选择题：看稀疏性还是看稳定性 ⭐⭐⭐

**题目：** 有两个数据分析任务：

1. 一共有 100 个变量，但你怀疑真正重要的只有 5 个；
2. 一共有 20 个变量，而且这些变量高度相关，但你认为它们多数都有贡献。

分别更推荐 Ridge 还是 LASSO？为什么？

**解题步骤：**

- 任务1：真实模型可能是**稀疏**的 → 选 **LASSO**
- 任务2：高相关且多数变量都有用 → 选 **Ridge**

**标准答法：**

> 第一个任务更适合 LASSO，因为 LASSO 能把一部分系数精确压缩为 0，从而做变量选择；第二个任务更适合 Ridge，因为 Ridge 不会把变量直接删掉，而是把高度相关变量的系数稳定地一起收缩，更适合“大家都有贡献”的场景。

**这题真正考什么：**
- LASSO 的关键词是 **稀疏、变量选择**
- Ridge 的关键词是 **稳定、共线性、整体收缩**

---

## 10.11 交叉验证与 One-SE Rule 题：不只会选最小，还要会选更简单 ⭐⭐⭐

**题目：** 某模型的交叉验证结果如下：

| $\lambda$ | CV误差 | 标准误SE |
|-----------|--------|---------|
| 0.01 | 10.0 | 0.2 |
| 0.10 | 9.2 | 0.3 |
| 1.00 | 9.4 | 0.2 |
| 10.00 | 10.6 | 0.4 |

回答：
1. `lambda.min` 该选哪个？
2. `lambda.1se` 该选哪个？

**解题步骤：**

- 最小 CV 误差是 9.2，对应 $\lambda=0.10$
  - 所以 `lambda.min = 0.10`
- One-SE Rule 阈值是：

$$9.2 + 0.3 = 9.5$$

- 在误差不超过 9.5 的候选里，取**最大的** $\lambda$
  - $\lambda=1.00$ 时 CV 误差是 9.4，满足要求
  - $\lambda=10.00$ 时误差 10.6，不满足
  - 所以 `lambda.1se = 1.00`

**标准答法：**

> `lambda.min` 选 0.10，因为它对应最小的交叉验证误差。`lambda.1se` 选 1.00，因为它是在“误差不超过最小值加 1 个标准误”的范围内所能选到的最大 $\lambda$，因此模型更简单、更稳定。

**这题真正考什么：**
- `lambda.min` = 最准
- `lambda.1se` = 更简单、更保守、更不容易过拟合

---

## 10.12 分类树 Gini 计算题：先算子节点，再做加权 ⭐⭐⭐

**题目：** 某节点共有 10 个样本，其中 6 个为 A 类，4 个为 B 类。现考虑一种分裂方式：

- 左节点：4 个 A，1 个 B
- 右节点：2 个 A，3 个 B

回答：
1. 父节点的 Gini 是多少？
2. 分裂后的加权 Gini 是多少？
3. 该分裂的 Gini 增益是多少？

**解题步骤：**

- 父节点：

$$G_{parent}=1-(0.6^2+0.4^2)=0.48$$

- 左节点：

$$G_L=1-(0.8^2+0.2^2)=0.32$$

- 右节点：

$$G_R=1-(0.4^2+0.6^2)=0.48$$

- 加权 Gini：

$$G_{split}=\frac{5}{10}\times 0.32 + \frac{5}{10}\times 0.48 = 0.40$$

- Gini 增益：

$$\Delta G = 0.48 - 0.40 = 0.08$$

**标准答法：**

> 父节点 Gini 为 0.48，分裂后的加权 Gini 为 0.40，因此该分裂带来的 Gini 增益为 0.08。分类树会优先选择使不纯度下降更多的分裂。

**这题真正考什么：**
- 先算每个节点的纯度，再按样本数加权
- Gini 越小越纯，增益越大越好

---

## 10.13 Bagging / RF / Boosting 对比题：最爱考的三兄弟 ⭐⭐⭐

**题目：** 回答下面三个问题：
1. 哪个方法能并行训练？
2. 哪个方法在节点分裂时随机抽特征？
3. 哪个方法通常用浅树、串行地拟合前一步残差？

**解题步骤：**

- Bagging：并行；每棵树独立；用全部特征
- Random Forest：并行；但**每次分裂时随机选 $m$ 个特征**
- Boosting：串行；每一步依赖前一步；常用浅树

**标准答法：**

> 能并行训练的是 Bagging 和 Random Forest；在节点分裂时随机抽特征的是 Random Forest；通常使用浅树并串行拟合前一步残差的是 Boosting。Bagging 和 RF 主要降低方差，而 Boosting 更侧重降低偏差。

**这题真正考什么：**
- Bagging = Bootstrap + Average
- RF = Bagging + 随机特征
- Boosting = 串行、残差、浅树

---

## 10.14 SVM 题：核函数、$C$ 和 $\gamma$ 一起考 ⭐⭐⭐

**题目：** 某 SVM 使用 RBF 核：

$$K(x,x') = \exp(-\gamma\|x-x'\|^2)$$

回答：
1. 这是什么核函数？
2. 如果同时把 $C$ 和 $\gamma$ 都调得很大，模型会怎样变化？

**解题步骤：**

- 第1步：认公式 → 这是 **径向基核（RBF）**
- 第2步：解释参数
  - $C$ 大：对误分类更敏感，边界更贴训练集
  - $\gamma$ 大：单个点影响范围更局部，边界更曲折

**标准答法：**

> 这是径向基核（RBF kernel）。若 $C$ 和 $\gamma$ 都很大，则模型会变得很复杂：$C$ 大意味着更不愿容忍训练误差，$\gamma$ 大意味着每个样本只在很局部范围内起作用，因此决策边界会更加弯曲，更容易过拟合。

**这题真正考什么：**
- 认公式
- 解释参数对边界复杂度的影响

---

## 10.15 样条与 GAM 题：看到 $(x-\xi)_+^3$ 就要想到结点 ⭐⭐

**题目：** 已知三次样条的一项为 $(x-5)_+^3$。

回答：
1. 当 $x=3$ 时，这一项等于多少？
2. 当 $x=8$ 时，这一项等于多少？
3. 为什么说这种写法能让模型变成“分段非线性”？

**解题步骤：**

- 根据定义：

$$ (x-5)_+^3 = \begin{cases}(x-5)^3,&x>5\\0,&x\le5\end{cases} $$

- 所以：
  - 当 $x=3$，该项为 0
  - 当 $x=8$，该项为 $(8-5)^3 = 27$

**标准答法：**

> 当 $x=3$ 时，这一项为 0；当 $x=8$ 时，这一项等于 27。之所以能形成分段非线性，是因为在结点左侧这一项完全不起作用，而在结点右侧才开始以三次项形式起作用，因此函数形状会在结点之后发生变化。

**顺带一问：GAM 为什么比黑箱模型更容易解释？**

> 因为 GAM 把模型写成 $f_1(X_1)+f_2(X_2)+\cdots$ 的形式，可以单独画出每个变量的边际效应，而不是把所有变量混在一起。

---

## 10.16 PCA 解释题：先算方差贡献率，再说符号不重要 ⭐⭐⭐

**题目：** 对 3 个标准化变量做 PCA，得到特征值：

$$\lambda_1 = 4, \qquad \lambda_2 = 1.5, \qquad \lambda_3 = 0.5$$

回答：
1. 第一主成分解释的方差比例是多少？
2. 前两个主成分累计解释多少方差？
3. 若某主成分载荷向量整体乘以 `-1`，含义会变吗？

**解题步骤：**

- 总方差：

$$4 + 1.5 + 0.5 = 6$$

- 第一主成分解释比例：

$$\frac{4}{6} = 0.667 \approx 66.7\%$$

- 前两个累计解释比例：

$$\frac{4+1.5}{6} = \frac{5.5}{6} \approx 91.7\%$$

**标准答法：**

> 第一主成分解释约 66.7% 的总方差，前两个主成分累计解释约 91.7% 的总方差。若载荷向量整体乘以 `-1`，主成分方向本质上没有变，只是坐标轴方向反了，因此统计含义不变。

**这题真正考什么：**
- 特征值 = 各主成分解释的方差大小
- 载荷向量的正负号整体翻转**不影响本质含义**

---

## 10.17 层次聚类题：linkage 特征与树状图怎么答 ⭐⭐

**题目：** 回答下面问题：
1. 哪种 linkage 容易出现链化（chain effect）？
2. 哪种 linkage 最常与倒置现象联系？
3. 如果树状图在高度 4 到 9 之间出现最大断层，通常应在哪一段切，为什么？

**解题步骤：**

- Single linkage → 最近点距离 → 容易链化
- Centroid linkage → 常与倒置现象联系
- 树状图出现最大断层时，通常在断层中间切开，得到更自然的聚类数

**标准答法：**

> Single linkage 最容易产生链化效应，因为它只看两类之间最近的点；Centroid linkage 最常与倒置现象联系。若树状图在高度 4 到 9 之间有最大断层，通常会在这段中间切开，因为这说明在此之前类内相对紧密，而跨过这段高度后合并会显著增大类间差异。

**这题真正考什么：**
- linkage 的个性记忆
- 树状图的核心不是“背图”，而是找**最大的合并跳跃**

---

## 10.18 Bootstrap 题：会不会从分位数读区间 ⭐⭐

**题目：** 某参数做了 1000 次 Bootstrap，得到：

- Bootstrap 平均估计值：2.05
- 2.5% 分位数：1.20
- 97.5% 分位数：2.80

回答：
1. 该参数的 95% Bootstrap percentile 区间是多少？
2. 若原假设是 $H_0: \theta = 0$，从这个区间看是否显著？
3. 单次 Bootstrap 抽样中，一个观测值没被抽中的概率大约是多少？

**解题步骤：**

- Percentile 区间直接取两端分位数：

$$[1.20, 2.80]$$

- 因为区间不包含 0，所以对 $\theta=0$ 的原假设不支持
- 单个观测没被抽中概率：

$$\left(1-\frac{1}{n}\right)^n \approx e^{-1} \approx 0.368$$

**标准答法：**

> 该参数的 95% Bootstrap percentile 区间为 `[1.20, 2.80]`。由于区间不包含 0，因此说明参数显著偏离 0。单次 Bootstrap 样本中，一个给定观测没有被抽中的概率大约为 0.368。

**这题真正考什么：**
- Bootstrap 题很多时候不是让你手搓重抽样，而是会不会**读区间、读标准误、认 0.368**

---

## 10.19 Boosting 代码题：看到 `shrinkage` 和 `n.trees` 怎么说 ⭐⭐

**题目：** 阅读下面代码，并说明各参数含义：

```r
fit <- gbm(y ~ ., data = train,
           distribution = "gaussian",
           n.trees = 5000,
           interaction.depth = 2,
           shrinkage = 0.01)
```

**解题步骤：**

- `gbm`：说明这是 **Gradient Boosting**
- `distribution = "gaussian"`：说明是**回归问题**
- `n.trees = 5000`：弱学习器总数
- `interaction.depth = 2`：每棵树最大深度约为 2，允许低阶交互
- `shrinkage = 0.01`：学习率，越小越稳，但通常需要更多树

**标准答法：**

> 这段代码在训练一个用于回归的 Gradient Boosting 模型。`n.trees` 表示 boosting 中树的数量；`interaction.depth` 表示每棵树的最大深度，用来控制单棵树的复杂度；`shrinkage` 表示学习率，越小则每棵树贡献越小、训练更稳，但通常需要更多棵树才能达到较好效果。

**这题真正考什么：**
- 一眼认出 `gbm`
- 学会把参数翻译成“树数 / 深度 / 学习率”

---

## 10.20 全部例题覆盖表

| 例题 | 对应模块 | 你应该练会什么 |
|------|---------|---------------|
| 10.1 | 线性回归 | 表格题的标准话术 |
| 10.2 | 逻辑回归 | log-odds、概率、OR |
| 10.3 | 分类评估 | 混淆矩阵推广 |
| 10.4 | 参数题 | 偏差-方差语言 |
| 10.5 | 聚类 | K-means 手算迭代 |
| 10.6 | 代码题 | `glmnet` 读法 |
| 10.7 | 交互项 | 边际效应、层次原则 |
| 10.8 | 回归区间 | CI vs PI |
| 10.9 | 分类模型 | LDA/QDA/NB/LogReg 选型 |
| 10.10 | 正则化 | Ridge vs LASSO 选型 |
| 10.11 | CV | `lambda.min` vs `lambda.1se` |
| 10.12 | 分类树 | Gini 计算 |
| 10.13 | 集成学习 | Bagging/RF/Boosting 对比 |
| 10.14 | SVM | 核函数、$C$、$\gamma$ |
| 10.15 | 样条/GAM | 截断幂基、可解释性 |
| 10.16 | PCA | 方差贡献率、载荷符号 |
| 10.17 | 层次聚类 | linkage 个性、树状图切割 |
| 10.18 | Bootstrap | percentile 区间、0.368 |
| 10.19 | Boosting代码 | `gbm` 参数翻译 |

> **推荐复习顺序：** 先刷 `10.1 ~ 10.4` 打牢主干题，再刷 `10.5 ~ 10.12` 解决计算与对比题，最后刷 `10.13 ~ 10.19` 补齐新增模块和代码题。
>
> **真正的刷题方法：** 每题至少做两遍。第一遍看会不会，第二遍练“标准话术”，第三遍训练自己在 30 秒内判断这题属于哪一种套路。

---

# 附录：核心公式速查卡

## 线性回归公式

$$\hat\beta_1=\frac{S_{xy}}{S_{xx}}, \quad \hat\beta_0=\bar y-\hat\beta_1\bar x$$

$$TSS=S_{yy}, \quad RSS=S_{yy}-\hat\beta_1 S_{xy} \text{（一元）}$$

$$R^2=1-\frac{RSS}{TSS}, \quad \bar R^2=1-(1-R^2)\frac{n-1}{n-p-1}$$

$$MSE=\frac{RSS}{n-p-1}, \quad SE(\hat\beta_1)=\sqrt{\frac{MSE}{S_{xx}}}$$

$$F=\frac{ESS/p}{RSS/(n-p-1)}, \quad VIF_j=\frac{1}{1-R_j^2}$$

## 分类公式

$$\delta_k(x)=x^T\Sigma^{-1}\mu_k-\frac{1}{2}\mu_k^T\Sigma^{-1}\mu_k+\log\pi_k \text{（LDA）}$$

$$\delta_k^Q(x)=-\frac{1}{2}\log|\Sigma_k|-\frac{1}{2}(x-\mu_k)^T\Sigma_k^{-1}(x-\mu_k)+\log\pi_k \text{（QDA）}$$

$$TPR=\frac{TP}{TP+FN}, \quad FPR=\frac{FP}{FP+TN}, \quad F1=\frac{2PR}{P+R}$$

## 正则化公式

$$\text{Ridge: }\hat\beta=(X^TX+\lambda I)^{-1}X^Ty$$

$$E[(y_0-\hat f)^2]=\text{Bias}^2+\text{Var}+\sigma^2$$

## 树与集成公式

$$G_m=1-\sum_k\hat p_{mk}^2 \text{（Gini）}, \quad H_m=-\sum_k\hat p_{mk}\log_2\hat p_{mk} \text{（熵）}$$

$$C_\alpha(T)=\text{Impurity}(T)+\alpha|T| \text{（代价复杂度）}$$

$$P(\text{OOB})\approx e^{-1}\approx 0.368$$

$$\alpha_b=\frac{1}{2}\ln\frac{1-\text{err}_b}{\text{err}_b} \text{（AdaBoost）}$$

$$f_b(x)=f_{b-1}(x)+\lambda T_b(x) \text{（Gradient Boosting）}$$

## 距离与SVM公式

$$\hat Y(x_0)=\mathrm{mode}\{Y_i:i\in \mathcal N_K(x_0)\} \text{（KNN分类）}$$

$$\min_{\beta_0,\beta,\xi}\ \frac{1}{2}\|\beta\|^2 + C\sum_{i=1}^n\xi_i\quad \text{s.t.}\quad y_i(\beta_0+\beta^Tx_i)\ge 1-\xi_i,\ \xi_i\ge0$$

$$K(x,x')=\exp(-\gamma\|x-x'\|^2) \text{（RBF核）}$$

## 非线性回归公式

$$f(x)=\beta_0+\beta_1x+\beta_2x^2+\beta_3x^3+\sum_{k=1}^K\beta_{k+3}(x-\xi_k)_+^3$$

$$\min_f\sum_{i=1}^n(y_i-f(x_i))^2+\lambda\int [f''(t)]^2dt \text{（光滑样条）}$$

$$Y=\beta_0+f_1(X_1)+\cdots+f_p(X_p)+\varepsilon \text{（GAM）}$$

## 降维与聚类公式

$$Z_1=\phi_{11}X_1+\phi_{21}X_2+\cdots+\phi_{p1}X_p \text{（第一主成分）}$$

$$\min_{C_1,\ldots,C_K}\sum_{k=1}^K\sum_{i\in C_k}\|x_i-\bar\mu_k\|^2 \text{（K均值目标）}$$

## 重抽样公式

$$SE_{boot}(\hat\theta)=\sqrt{\frac{1}{B-1}\sum_{b=1}^B(\hat\theta^{*(b)}-\bar\theta^*)^2}$$

$$\left(1-\frac{1}{n}\right)^n\approx e^{-1}\approx 0.368$$

## 逻辑回归公式

$$\log\frac{p}{1-p}=\beta_0+\beta_1X_1+\cdots+\beta_pX_p$$

$$p=\frac{e^{\beta_0+\beta_1X_1+\cdots}}{1+e^{\beta_0+\beta_1X_1+\cdots}}, \quad OR=e^\beta$$

---

> **考前最后提醒：**
>
> 1. **表格题（最高频）：** 先看p值判显著 → 看系数正负判方向 → 多元模型必须补"其他变量保持不变"。
> 2. **计算题重点：** 混淆矩阵推广、逻辑回归概率、K均值迭代、光滑样条 $\lambda$、AdaBoost权重更新。
> 3. **对比题（必考）：** "Bagging/RF/Boosting"、"Ridge/LASSO"、"LDA/QDA"、"KNN/SVM/逻辑回归"，尽量用表格答。
> 4. **代码题：** 重点认 `glmnet`、`cv.glmnet`、`gbm`、`kmeans`、`hclust`、`prcomp`、`boot`，先说模型，再说参数，再说指标。
> 5. **参数题：** 看到 $\lambda$、$C$、$K$、$\gamma$、树数 $B$，第一反应就是说明模型复杂度与偏差-方差如何变化。
